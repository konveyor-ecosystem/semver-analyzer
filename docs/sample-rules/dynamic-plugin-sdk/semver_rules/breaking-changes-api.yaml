- ruleID: semver-packages-lib-core-src-index-pluginstore-d-ts-pluginstore-constructor-type-changed
  labels:
  - source=semver-analyzer
  - change-type=type-changed
  - kind=method
  - has-codemod=true
  - package=@openshift/dynamic-plugin-sdk
  effort: 3
  category: mandatory
  description: Parameter `options` of `constructor` changed type from `PluginStoreOptions` to `PluginStoreLoaderSettings & PluginStoreOptions`
  message: |-
    Method 'PluginStore.constructor' had its type changed (method): Parameter `options` of `constructor` changed type from `PluginStoreOptions` to `PluginStoreLoaderSettings & PluginStoreOptions`

    File: packages/lib-core/src/index.PluginStore.d.ts
    Before: PluginStoreOptions
    After: PluginStoreLoaderSettings & PluginStoreOptions
  when:
    frontend.referenced:
      pattern: ^constructor$
      location: FUNCTION_CALL
      from: '@openshift/dynamic-plugin-sdk'
- ruleID: semver-packages-lib-core-src-types-common-src-errors-errorwithcause-errorwithcause-d-ts-errorwithcause-constructor-type-changed
  labels:
  - source=semver-analyzer
  - change-type=type-changed
  - kind=method
  - has-codemod=true
  - package=@openshift/dynamic-plugin-sdk
  effort: 3
  category: mandatory
  description: Parameter `cause` of `constructor` was made required
  message: |-
    Method 'ErrorWithCause.constructor' had its type changed (method): Parameter `cause` of `constructor` was made required

    File: packages/lib-core/src/types/common/src/errors/ErrorWithCause.ErrorWithCause.d.ts
    Before: optional
    After: required
  when:
    frontend.referenced:
      pattern: ^constructor$
      location: FUNCTION_CALL
      from: '@openshift/dynamic-plugin-sdk'
- ruleID: semver-packages-lib-core-src-types-lib-core-src-runtime-pluginloader-pluginloader-d-ts-pluginloader-processpluginmanifest-renamed
  labels:
  - source=semver-analyzer
  - change-type=renamed
  - kind=method
  - has-codemod=true
  - package=@openshift/dynamic-plugin-sdk
  effort: 1
  category: mandatory
  description: method `processPluginManifest` was renamed to `transformPluginManifest` in `PluginLoader`
  message: |-
    Method 'PluginLoader.processPluginManifest' was renamed (method): method `processPluginManifest` was renamed to `transformPluginManifest` in `PluginLoader`

    File: packages/lib-core/src/types/lib-core/src/runtime/PluginLoader.PluginLoader.d.ts
    Before: processPluginManifest
    After: transformPluginManifest
  when:
    frontend.referenced:
      pattern: ^processPluginManifest$
      location: FUNCTION_CALL
      from: '@openshift/dynamic-plugin-sdk'
- ruleID: semver-packages-lib-core-src-types-lib-core-src-runtime-pluginloader-pluginloader-d-ts-pluginloader-loadpluginmanifest-type-changed
  labels:
  - source=semver-analyzer
  - change-type=type-changed
  - kind=method
  - has-codemod=true
  - package=@openshift/dynamic-plugin-sdk
  effort: 3
  category: mandatory
  description: '`loadPluginManifest` was made sync (async wrapper removed)'
  message: |-
    Method 'PluginLoader.loadPluginManifest' had its type changed (method): `loadPluginManifest` was made sync (async wrapper removed)

    File: packages/lib-core/src/types/lib-core/src/runtime/PluginLoader.PluginLoader.d.ts
    Before: Promise<PluginManifest>
    After: Promise
  when:
    frontend.referenced:
      pattern: ^loadPluginManifest$
      location: FUNCTION_CALL
      from: '@openshift/dynamic-plugin-sdk'
- ruleID: semver-packages-lib-core-src-types-lib-core-src-runtime-pluginloader-d-ts-pluginloaderoptions-type-changed
  labels:
  - source=semver-analyzer
  - change-type=type-changed
  - kind=type-alias
  - has-codemod=true
  - package=@openshift/dynamic-plugin-sdk
  - change-scope=additive
  effort: 3
  category: mandatory
  description: |-
    Return type of `PluginLoaderOptions` changed from `Partial<{
        /**
         * Control which plugins can be loaded.
         *
         * The `reload` argument indicates whether an already loaded plugin is to be reloaded.
         *
         * By default, all plugins are allowed to be loaded.
         */
        canLoadPlugin: (manifest: PluginManifest, reload: boolean) => boolean;
        /**
         * Control whether the given plugin script can be reloaded when attempting to reload
         * the associated plugin.
         *
         * By default, all plugin scripts are allowed to be reloaded.
         */
        canReloadScript: (manifest: PluginManifest, scriptName: string) => boolean;
        /**
         * Customize the global callback function used by plugin entry scripts.
         *
         * This option applies only to plugins using the `callback` registration method.
         */
        entryCallbackSettings: Partial<{
            /**
             * Control whether to register the callback function.
             *
             * Default value: `true`.
             */
            registerCallback: boolean;
            /**
             * Name of the callback function.
             *
             * Default value: `__load_plugin_entry__`.
             */
            name: string;
        }>;
        /**
         * Custom resource fetch implementation.
         *
         * The custom implementation may specify any host application or environment specific
         * request headers that are necessary to fetch plugin resources over the network.
         *
         * By default, a basic {@link fetch} API based implementation is used.
         */
        fetchImpl: ResourceFetch;
        /**
         * Fixed resolutions used when processing plugin dependencies.
         *
         * There are two kinds of dependencies:
         * - Regular dependencies, which refer to actual plugins that must be loaded with
         *   the right version prior to loading the plugin that depends on them.
         * - Environment dependencies, which refer to any environment specific constants
         *   such as the host application's version, runtime platform version, etc.
         *
         * This option should be used to satisfy any environment specific dependencies
         * supported by the host application.
         *
         * Default value: empty object.
         */
        fixedPluginDependencyResolutions: Record<string, string>;
        /**
         * webpack share scope object for initializing `PluginEntryModule` containers.
         *
         * Host applications built with webpack should use dedicated webpack specific APIs
         * such as `__webpack_init_sharing__` and `__webpack_share_scopes__` to initialize
         * and access this object.
         *
         * Default value: empty object.
         *
         * @see https://webpack.js.org/concepts/module-federation/#dynamic-remote-containers
         */
        sharedScope: AnyObject;
        /**
         * Post-process the plugin manifest.
         *
         * By default, no post-processing is performed on the manifest.
         */
        postProcessManifest: (manifest: PluginManifest) => PluginManifest;
        /**
         * Provide access to the plugin's entry module.
         *
         * This option applies only to plugins using the `custom` registration method.
         *
         * For example, if a plugin was built with `var` library type (i.e. its entry module
         * is assigned to a variable), you can access the entry module via `window[pluginName]`.
         *
         * By default, this function does nothing.
         */
        getPluginEntryModule: (manifest: PluginManifest) => PluginEntryModule | void;
    }>` to `Partial<{
        /**
         * Control which plugins can be loaded.
         *
         * The `reload` argument indicates whether an already loaded plugin is to be reloaded.
         *
         * By default, all plugins are allowed to be loaded.
         */
        canLoadPlugin: (manifest: PluginManifest, reload: boolean) => boolean;
        /**
         * Control whether the given plugin script can be reloaded when attempting to reload
         * the associated plugin.
         *
         * By default, all plugin scripts are allowed to be reloaded.
         */
        canReloadScript: (manifest: PluginManifest, scriptName: string) => boolean;
        /**
         * Customize the global callback function used by plugin entry scripts.
         *
         * This option applies only to plugins using the `callback` registration method.
         */
        entryCallbackSettings: Partial<{
            /**
             * Control whether to register the callback function.
             *
             * Default value: `true`.
             */
            registerCallback: boolean;
            /**
             * Name of the callback function.
             *
             * Default value: `__load_plugin_entry__`.
             */
            name: string;
        }>;
        /**
         * Custom resource fetch implementation.
         *
         * The custom implementation may specify any host application or environment specific
         * request headers that are necessary to fetch plugin resources over the network.
         *
         * By default, a basic {@link fetch} API based implementation is used.
         */
        fetchImpl: ResourceFetch;
        /**
         * Fixed resolutions used when processing plugin dependencies.
         *
         * There are two kinds of dependencies:
         * - Regular dependencies, which refer to actual plugins that must be loaded with
         *   the right version prior to loading the plugin that depends on them.
         * - Environment dependencies, which refer to any environment specific constants
         *   such as the host application's version, runtime platform version, etc.
         *
         * This option should be used to satisfy any environment specific dependencies
         * supported by the host application.
         *
         * Default value: empty object.
         */
        fixedPluginDependencyResolutions: Record<string, string>;
        /**
         * webpack share scope object for initializing `PluginEntryModule` containers.
         *
         * Host applications built with webpack should use dedicated webpack specific APIs
         * such as `__webpack_init_sharing__` and `__webpack_share_scopes__` to initialize
         * and access this object.
         *
         * Default value: empty object.
         *
         * @see https://webpack.js.org/concepts/module-federation/#dynamic-remote-containers
         */
        sharedScope: AnyObject;
        /**
         * Transform the plugin manifest before loading the associated plugin.
         *
         * By default, no transformation is performed on the manifest.
         */
        transformPluginManifest: (manifest: PluginManifest) => PluginManifest;
        /**
         * Provide access to the plugin's entry module.
         *
         * This option applies only to plugins using the `custom` registration method.
         *
         * For example, if a plugin was built with `var` library type (i.e. its entry module
         * is assigned to a variable), you can access the entry module via `window[pluginName]`.
         *
         * By default, this function does nothing.
         */
        getPluginEntryModule: (manifest: PluginManifest) => PluginEntryModule | void;
    }>`
  message: |-
    Type-alias 'PluginLoaderOptions' had its type changed (type-alias): Return type of `PluginLoaderOptions` changed from `Partial<{
        /**
         * Control which plugins can be loaded.
         *
         * The `reload` argument indicates whether an already loaded plugin is to be reloaded.
         *
         * By default, all plugins are allowed to be loaded.
         */
        canLoadPlugin: (manifest: PluginManifest, reload: boolean) => boolean;
        /**
         * Control whether the given plugin script can be reloaded when attempting to reload
         * the associated plugin.
         *
         * By default, all plugin scripts are allowed to be reloaded.
         */
        canReloadScript: (manifest: PluginManifest, scriptName: string) => boolean;
        /**
         * Customize the global callback function used by plugin entry scripts.
         *
         * This option applies only to plugins using the `callback` registration method.
         */
        entryCallbackSettings: Partial<{
            /**
             * Control whether to register the callback function.
             *
             * Default value: `true`.
             */
            registerCallback: boolean;
            /**
             * Name of the callback function.
             *
             * Default value: `__load_plugin_entry__`.
             */
            name: string;
        }>;
        /**
         * Custom resource fetch implementation.
         *
         * The custom implementation may specify any host application or environment specific
         * request headers that are necessary to fetch plugin resources over the network.
         *
         * By default, a basic {@link fetch} API based implementation is used.
         */
        fetchImpl: ResourceFetch;
        /**
         * Fixed resolutions used when processing plugin dependencies.
         *
         * There are two kinds of dependencies:
         * - Regular dependencies, which refer to actual plugins that must be loaded with
         *   the right version prior to loading the plugin that depends on them.
         * - Environment dependencies, which refer to any environment specific constants
         *   such as the host application's version, runtime platform version, etc.
         *
         * This option should be used to satisfy any environment specific dependencies
         * supported by the host application.
         *
         * Default value: empty object.
         */
        fixedPluginDependencyResolutions: Record<string, string>;
        /**
         * webpack share scope object for initializing `PluginEntryModule` containers.
         *
         * Host applications built with webpack should use dedicated webpack specific APIs
         * such as `__webpack_init_sharing__` and `__webpack_share_scopes__` to initialize
         * and access this object.
         *
         * Default value: empty object.
         *
         * @see https://webpack.js.org/concepts/module-federation/#dynamic-remote-containers
         */
        sharedScope: AnyObject;
        /**
         * Post-process the plugin manifest.
         *
         * By default, no post-processing is performed on the manifest.
         */
        postProcessManifest: (manifest: PluginManifest) => PluginManifest;
        /**
         * Provide access to the plugin's entry module.
         *
         * This option applies only to plugins using the `custom` registration method.
         *
         * For example, if a plugin was built with `var` library type (i.e. its entry module
         * is assigned to a variable), you can access the entry module via `window[pluginName]`.
         *
         * By default, this function does nothing.
         */
        getPluginEntryModule: (manifest: PluginManifest) => PluginEntryModule | void;
    }>` to `Partial<{
        /**
         * Control which plugins can be loaded.
         *
         * The `reload` argument indicates whether an already loaded plugin is to be reloaded.
         *
         * By default, all plugins are allowed to be loaded.
         */
        canLoadPlugin: (manifest: PluginManifest, reload: boolean) => boolean;
        /**
         * Control whether the given plugin script can be reloaded when attempting to reload
         * the associated plugin.
         *
         * By default, all plugin scripts are allowed to be reloaded.
         */
        canReloadScript: (manifest: PluginManifest, scriptName: string) => boolean;
        /**
         * Customize the global callback function used by plugin entry scripts.
         *
         * This option applies only to plugins using the `callback` registration method.
         */
        entryCallbackSettings: Partial<{
            /**
             * Control whether to register the callback function.
             *
             * Default value: `true`.
             */
            registerCallback: boolean;
            /**
             * Name of the callback function.
             *
             * Default value: `__load_plugin_entry__`.
             */
            name: string;
        }>;
        /**
         * Custom resource fetch implementation.
         *
         * The custom implementation may specify any host application or environment specific
         * request headers that are necessary to fetch plugin resources over the network.
         *
         * By default, a basic {@link fetch} API based implementation is used.
         */
        fetchImpl: ResourceFetch;
        /**
         * Fixed resolutions used when processing plugin dependencies.
         *
         * There are two kinds of dependencies:
         * - Regular dependencies, which refer to actual plugins that must be loaded with
         *   the right version prior to loading the plugin that depends on them.
         * - Environment dependencies, which refer to any environment specific constants
         *   such as the host application's version, runtime platform version, etc.
         *
         * This option should be used to satisfy any environment specific dependencies
         * supported by the host application.
         *
         * Default value: empty object.
         */
        fixedPluginDependencyResolutions: Record<string, string>;
        /**
         * webpack share scope object for initializing `PluginEntryModule` containers.
         *
         * Host applications built with webpack should use dedicated webpack specific APIs
         * such as `__webpack_init_sharing__` and `__webpack_share_scopes__` to initialize
         * and access this object.
         *
         * Default value: empty object.
         *
         * @see https://webpack.js.org/concepts/module-federation/#dynamic-remote-containers
         */
        sharedScope: AnyObject;
        /**
         * Transform the plugin manifest before loading the associated plugin.
         *
         * By default, no transformation is performed on the manifest.
         */
        transformPluginManifest: (manifest: PluginManifest) => PluginManifest;
        /**
         * Provide access to the plugin's entry module.
         *
         * This option applies only to plugins using the `custom` registration method.
         *
         * For example, if a plugin was built with `var` library type (i.e. its entry module
         * is assigned to a variable), you can access the entry module via `window[pluginName]`.
         *
         * By default, this function does nothing.
         */
        getPluginEntryModule: (manifest: PluginManifest) => PluginEntryModule | void;
    }>`

    File: packages/lib-core/src/types/lib-core/src/runtime/PluginLoader.d.ts
    Before: Partial<{
        /**
         * Control which plugins can be loaded.
         *
         * The `reload` argument indicates whether an already loaded plugin is to be reloaded.
         *
         * By default, all plugins are allowed to be loaded.
         */
        canLoadPlugin: (manifest: PluginManifest, reload: boolean) => boolean;
        /**
         * Control whether the given plugin script can be reloaded when attempting to reload
         * the associated plugin.
         *
         * By default, all plugin scripts are allowed to be reloaded.
         */
        canReloadScript: (manifest: PluginManifest, scriptName: string) => boolean;
        /**
         * Customize the global callback function used by plugin entry scripts.
         *
         * This option applies only to plugins using the `callback` registration method.
         */
        entryCallbackSettings: Partial<{
            /**
             * Control whether to register the callback function.
             *
             * Default value: `true`.
             */
            registerCallback: boolean;
            /**
             * Name of the callback function.
             *
             * Default value: `__load_plugin_entry__`.
             */
            name: string;
        }>;
        /**
         * Custom resource fetch implementation.
         *
         * The custom implementation may specify any host application or environment specific
         * request headers that are necessary to fetch plugin resources over the network.
         *
         * By default, a basic {@link fetch} API based implementation is used.
         */
        fetchImpl: ResourceFetch;
        /**
         * Fixed resolutions used when processing plugin dependencies.
         *
         * There are two kinds of dependencies:
         * - Regular dependencies, which refer to actual plugins that must be loaded with
         *   the right version prior to loading the plugin that depends on them.
         * - Environment dependencies, which refer to any environment specific constants
         *   such as the host application's version, runtime platform version, etc.
         *
         * This option should be used to satisfy any environment specific dependencies
         * supported by the host application.
         *
         * Default value: empty object.
         */
        fixedPluginDependencyResolutions: Record<string, string>;
        /**
         * webpack share scope object for initializing `PluginEntryModule` containers.
         *
         * Host applications built with webpack should use dedicated webpack specific APIs
         * such as `__webpack_init_sharing__` and `__webpack_share_scopes__` to initialize
         * and access this object.
         *
         * Default value: empty object.
         *
         * @see https://webpack.js.org/concepts/module-federation/#dynamic-remote-containers
         */
        sharedScope: AnyObject;
        /**
         * Post-process the plugin manifest.
         *
         * By default, no post-processing is performed on the manifest.
         */
        postProcessManifest: (manifest: PluginManifest) => PluginManifest;
        /**
         * Provide access to the plugin's entry module.
         *
         * This option applies only to plugins using the `custom` registration method.
         *
         * For example, if a plugin was built with `var` library type (i.e. its entry module
         * is assigned to a variable), you can access the entry module via `window[pluginName]`.
         *
         * By default, this function does nothing.
         */
        getPluginEntryModule: (manifest: PluginManifest) => PluginEntryModule | void;
    }>
    After: Partial<{
        /**
         * Control which plugins can be loaded.
         *
         * The `reload` argument indicates whether an already loaded plugin is to be reloaded.
         *
         * By default, all plugins are allowed to be loaded.
         */
        canLoadPlugin: (manifest: PluginManifest, reload: boolean) => boolean;
        /**
         * Control whether the given plugin script can be reloaded when attempting to reload
         * the associated plugin.
         *
         * By default, all plugin scripts are allowed to be reloaded.
         */
        canReloadScript: (manifest: PluginManifest, scriptName: string) => boolean;
        /**
         * Customize the global callback function used by plugin entry scripts.
         *
         * This option applies only to plugins using the `callback` registration method.
         */
        entryCallbackSettings: Partial<{
            /**
             * Control whether to register the callback function.
             *
             * Default value: `true`.
             */
            registerCallback: boolean;
            /**
             * Name of the callback function.
             *
             * Default value: `__load_plugin_entry__`.
             */
            name: string;
        }>;
        /**
         * Custom resource fetch implementation.
         *
         * The custom implementation may specify any host application or environment specific
         * request headers that are necessary to fetch plugin resources over the network.
         *
         * By default, a basic {@link fetch} API based implementation is used.
         */
        fetchImpl: ResourceFetch;
        /**
         * Fixed resolutions used when processing plugin dependencies.
         *
         * There are two kinds of dependencies:
         * - Regular dependencies, which refer to actual plugins that must be loaded with
         *   the right version prior to loading the plugin that depends on them.
         * - Environment dependencies, which refer to any environment specific constants
         *   such as the host application's version, runtime platform version, etc.
         *
         * This option should be used to satisfy any environment specific dependencies
         * supported by the host application.
         *
         * Default value: empty object.
         */
        fixedPluginDependencyResolutions: Record<string, string>;
        /**
         * webpack share scope object for initializing `PluginEntryModule` containers.
         *
         * Host applications built with webpack should use dedicated webpack specific APIs
         * such as `__webpack_init_sharing__` and `__webpack_share_scopes__` to initialize
         * and access this object.
         *
         * Default value: empty object.
         *
         * @see https://webpack.js.org/concepts/module-federation/#dynamic-remote-containers
         */
        sharedScope: AnyObject;
        /**
         * Transform the plugin manifest before loading the associated plugin.
         *
         * By default, no transformation is performed on the manifest.
         */
        transformPluginManifest: (manifest: PluginManifest) => PluginManifest;
        /**
         * Provide access to the plugin's entry module.
         *
         * This option applies only to plugins using the `custom` registration method.
         *
         * For example, if a plugin was built with `var` library type (i.e. its entry module
         * is assigned to a variable), you can access the entry module via `window[pluginName]`.
         *
         * By default, this function does nothing.
         */
        getPluginEntryModule: (manifest: PluginManifest) => PluginEntryModule | void;
    }>
  when:
    or:
    - frontend.referenced:
        pattern: ^PluginLoaderOptions$
        location: TYPE_REFERENCE
        from: '@openshift/dynamic-plugin-sdk'
    - frontend.referenced:
        pattern: ^PluginLoaderOptions$
        location: IMPORT
        from: '@openshift/dynamic-plugin-sdk'
- ruleID: semver-packages-lib-core-src-types-lib-core-src-runtime-pluginstore-pluginstore-d-ts-pluginstore-constructor-type-changed
  labels:
  - source=semver-analyzer
  - change-type=type-changed
  - kind=method
  - has-codemod=true
  - package=@openshift/dynamic-plugin-sdk
  effort: 3
  category: mandatory
  description: Parameter `options` of `constructor` changed type from `PluginStoreOptions` to `PluginStoreLoaderSettings & PluginStoreOptions`
  message: |-
    Method 'PluginStore.constructor' had its type changed (method): Parameter `options` of `constructor` changed type from `PluginStoreOptions` to `PluginStoreLoaderSettings & PluginStoreOptions`

    File: packages/lib-core/src/types/lib-core/src/runtime/PluginStore.PluginStore.d.ts
    Before: PluginStoreOptions
    After: PluginStoreLoaderSettings & PluginStoreOptions
  when:
    frontend.referenced:
      pattern: ^constructor$
      location: FUNCTION_CALL
      from: '@openshift/dynamic-plugin-sdk'
- ruleID: semver-packages-lib-core-src-types-lib-core-src-runtime-pluginstore-d-ts-pluginstoreoptions-type-changed
  labels:
  - source=semver-analyzer
  - change-type=type-changed
  - kind=type-alias
  - has-codemod=false
  - package=@openshift/dynamic-plugin-sdk
  effort: 3
  category: mandatory
  description: |-
    Return type of `PluginStoreOptions` changed from `Partial<{
        /**
         * Options passed to `PluginLoader` instance managed by the `PluginStore`.
         *
         * Default value: empty object.
         */
        loaderOptions: PluginLoaderOptions;
        /**
         * Control whether to enable plugins automatically once they are loaded.
         *
         * Default value: `true`.
         */
        autoEnableLoadedPlugins: boolean;
    }>` to `Partial<{
        /**
         * Control whether to enable plugins automatically once they are loaded.
         *
         * Default value: `true`.
         */
        autoEnableLoadedPlugins: boolean;
    }>`
  message: |-
    Type-alias 'PluginStoreOptions' had its type changed (type-alias): Return type of `PluginStoreOptions` changed from `Partial<{
        /**
         * Options passed to `PluginLoader` instance managed by the `PluginStore`.
         *
         * Default value: empty object.
         */
        loaderOptions: PluginLoaderOptions;
        /**
         * Control whether to enable plugins automatically once they are loaded.
         *
         * Default value: `true`.
         */
        autoEnableLoadedPlugins: boolean;
    }>` to `Partial<{
        /**
         * Control whether to enable plugins automatically once they are loaded.
         *
         * Default value: `true`.
         */
        autoEnableLoadedPlugins: boolean;
    }>`

    File: packages/lib-core/src/types/lib-core/src/runtime/PluginStore.d.ts
    Before: Partial<{
        /**
         * Options passed to `PluginLoader` instance managed by the `PluginStore`.
         *
         * Default value: empty object.
         */
        loaderOptions: PluginLoaderOptions;
        /**
         * Control whether to enable plugins automatically once they are loaded.
         *
         * Default value: `true`.
         */
        autoEnableLoadedPlugins: boolean;
    }>
    After: Partial<{
        /**
         * Control whether to enable plugins automatically once they are loaded.
         *
         * Default value: `true`.
         */
        autoEnableLoadedPlugins: boolean;
    }>
  when:
    or:
    - frontend.referenced:
        pattern: ^PluginStoreOptions$
        location: TYPE_REFERENCE
        from: '@openshift/dynamic-plugin-sdk'
    - frontend.referenced:
        pattern: ^PluginStoreOptions$
        location: IMPORT
        from: '@openshift/dynamic-plugin-sdk'
- ruleID: semver-packages-lib-core-src-types-lib-core-src-runtime-pluginstorecontext-d-ts-pluginstoreproviderprops-type-changed
  labels:
  - source=semver-analyzer
  - change-type=type-changed
  - kind=type-alias
  - has-codemod=true
  - package=@openshift/dynamic-plugin-sdk
  effort: 3
  category: mandatory
  description: |-
    Return type of `PluginStoreProviderProps` changed from `React.PropsWithChildren<{
        store: PluginStore;
    }>` to `React.PropsWithChildren<{
        store: PluginStoreInterface;
    }>`
  message: |-
    Type-alias 'PluginStoreProviderProps' had its type changed (type-alias): Return type of `PluginStoreProviderProps` changed from `React.PropsWithChildren<{
        store: PluginStore;
    }>` to `React.PropsWithChildren<{
        store: PluginStoreInterface;
    }>`

    File: packages/lib-core/src/types/lib-core/src/runtime/PluginStoreContext.d.ts
    Before: React.PropsWithChildren<{
        store: PluginStore;
    }>
    After: React.PropsWithChildren<{
        store: PluginStoreInterface;
    }>
  when:
    or:
    - frontend.referenced:
        pattern: ^PluginStoreProviderProps$
        location: TYPE_REFERENCE
        from: '@openshift/dynamic-plugin-sdk'
    - frontend.referenced:
        pattern: ^PluginStoreProviderProps$
        location: IMPORT
        from: '@openshift/dynamic-plugin-sdk'
- ruleID: semver-packages-lib-core-src-types-lib-core-src-runtime-useresolvedextensions-d-ts-useresolvedextensions-type-changed
  labels:
  - source=semver-analyzer
  - change-type=type-changed
  - kind=constant
  - has-codemod=true
  - package=@openshift/dynamic-plugin-sdk
  effort: 3
  category: mandatory
  description: 'Return type of `useResolvedExtensions` changed from `<TExtension extends Extension<string, AnyObject>>(predicate?: ExtensionPredicate<TExtension> | undefined) => UseResolvedExtensionsResult<TExtension>` to `<TExtension extends Extension<string, AnyObject>>(predicate?: ExtensionPredicate<TExtension> | undefined, options?: UseResolvedExtensionsOptions) => UseResolvedExtensionsResult<TExtension>`'
  message: |-
    Constant 'useResolvedExtensions' had its type changed (constant): Return type of `useResolvedExtensions` changed from `<TExtension extends Extension<string, AnyObject>>(predicate?: ExtensionPredicate<TExtension> | undefined) => UseResolvedExtensionsResult<TExtension>` to `<TExtension extends Extension<string, AnyObject>>(predicate?: ExtensionPredicate<TExtension> | undefined, options?: UseResolvedExtensionsOptions) => UseResolvedExtensionsResult<TExtension>`

    File: packages/lib-core/src/types/lib-core/src/runtime/useResolvedExtensions.d.ts
    Before: <TExtension extends Extension<string, AnyObject>>(predicate?: ExtensionPredicate<TExtension> | undefined) => UseResolvedExtensionsResult<TExtension>
    After: <TExtension extends Extension<string, AnyObject>>(predicate?: ExtensionPredicate<TExtension> | undefined, options?: UseResolvedExtensionsOptions) => UseResolvedExtensionsResult<TExtension>
  when:
    frontend.referenced:
      pattern: ^useResolvedExtensions$
      location: IMPORT
      from: '@openshift/dynamic-plugin-sdk'
- ruleID: semver-packages-lib-core-src-types-lib-core-src-types-store-d-ts-pluginstoreinterface-type-changed
  labels:
  - source=semver-analyzer
  - change-type=type-changed
  - kind=type-alias
  - has-codemod=true
  - package=@openshift/dynamic-plugin-sdk
  - change-scope=additive
  effort: 3
  category: mandatory
  description: |-
    Return type of `PluginStoreInterface` changed from `{
        /**
         * Version of the `@openshift/dynamic-plugin-sdk` package.
         */
        readonly sdkVersion: string;
        /**
         * Subscribe to events emitted by the `PluginStore`.
         *
         * See {@link PluginEventType} for information on specific event types.
         *
         * Returns a function for unsubscribing the provided listener.
         */
        subscribe: (eventTypes: PluginEventType[], listener: VoidFunction) => VoidFunction;
        /**
         * Get extensions which are currently in use.
         *
         * An extension is in use when the associated plugin is currently enabled and its
         * feature flag requirements (if any) are met according to current feature flags.
         *
         * If you need to enhance or modify existing extension objects after the associated
         * plugin has been loaded and processed, we recommend using a custom React hook which
         * calls `useExtensions` or `useResolvedExtensions` and returns new extension object
         * instances.
         *
         * This method always returns a new array instance.
         */
        getExtensions: () => LoadedExtension[];
        /**
         * Get current information about plugins.
         *
         * This method always returns a new array instance.
         */
        getPluginInfo: () => PluginInfoEntry[];
        /**
         * Get current feature flags.
         *
         * This method always returns a new object.
         */
        getFeatureFlags: () => FeatureFlags;
        /**
         * Merge the entries of `newFlags` with current feature flags (non-boolean values
         * will be discarded).
         */
        setFeatureFlags: (newFlags: FeatureFlags) => void;
        /**
         * Start loading a plugin from the given manifest.
         *
         * The plugin manifest can be provided directly as an object or referenced via URL.
         *
         * Depending on the plugin's current load status, this method works as follows:
         * - plugin is loading - do nothing
         * - plugin has been loaded - reload only if `forceReload` is `true`
         * - plugin has failed to load - always reload
         *
         * The resulting Promise resolves when the load operation is complete. If the given
         * plugin is still loading when this method is invoked, the same Promise instance that
         * represents the load operation is returned.
         *
         * Use the `subscribe` method to respond to events emitted by the `PluginStore`.
         *
         * Be advised that any plugin modules which are already loaded by the host application
         * will _not_ be replaced upon reloading the associated plugin. This is due to webpack
         * module caching which also applies to federated modules. If a host application detects
         * changes in a plugin's deployment, users should be prompted to reload the application
         * to ensure all plugin modules in use are up to date.
         */
        loadPlugin: (manifest: PluginManifest | string, forceReload?: boolean) => Promise<void>;
        /**
         * Enable the given plugin(s).
         *
         * Enabling a plugin puts all of its extensions into use.
         */
        enablePlugins: (pluginNames: string[]) => void;
        /**
         * Disable the given plugin(s) with an optional reason.
         *
         * Disabling a plugin puts all of its extensions out of use.
         */
        disablePlugins: (pluginNames: string[], disableReason?: string) => void;
        /**
         * Get a specific module exposed by the given plugin.
         */
        getExposedModule: <TModule extends AnyObject>(pluginName: string, moduleName: string) => Promise<TModule>;
    }` to `{
        /**
         * Version of the `@openshift/dynamic-plugin-sdk` package.
         */
        readonly sdkVersion: string;
        /**
         * Subscribe to events emitted by the `PluginStore`.
         *
         * See {@link PluginEventType} for information on specific event types.
         *
         * Returns a function for unsubscribing the provided listener.
         */
        subscribe: (eventTypes: PluginEventType[], listener: VoidFunction) => VoidFunction;
        /**
         * Get extensions which are currently in use.
         *
         * An extension is in use when the associated plugin is currently enabled and its
         * feature flag requirements (if any) are met according to current feature flags.
         *
         * If you need to enhance or modify existing extension objects after the associated
         * plugin has been loaded and processed, we recommend using a custom React hook which
         * calls `useExtensions` or `useResolvedExtensions` and returns new extension object
         * instances.
         *
         * This method always returns a new array instance.
         */
        getExtensions: () => LoadedExtension[];
        /**
         * Get current information about plugins.
         *
         * This method always returns a new array instance.
         */
        getPluginInfo: () => PluginInfoEntry[];
        /**
         * Get current feature flags.
         *
         * This method always returns a new object.
         */
        getFeatureFlags: () => FeatureFlags;
        /**
         * Merge the entries of `newFlags` with current feature flags (non-boolean values
         * will be discarded).
         */
        setFeatureFlags: (newFlags: FeatureFlags) => void;
        /**
         * Start loading a plugin from the given manifest.
         *
         * The plugin manifest can be provided directly as an object or referenced via URL.
         *
         * Depending on the plugin's current load status, this method works as follows:
         * - plugin is loading - do nothing
         * - plugin has been loaded - reload only if `forceReload` is `true`
         * - plugin has failed to load - always reload
         *
         * The resulting Promise resolves when the load operation is complete. If the given
         * plugin is still loading when this method is invoked, the same Promise instance that
         * represents the load operation is returned.
         *
         * The resulting Promise rejects only when the plugin manifest cannot be loaded or
         * processed by the `PluginLoader` implementation.
         *
         * Use the `subscribe` method to respond to events emitted by the `PluginStore`.
         *
         * Be advised that any plugin modules which are already loaded by the host application
         * will _not_ be replaced upon reloading the associated plugin. This is due to webpack
         * module caching which also applies to federated modules. If a host application detects
         * changes in a plugin's deployment, users should be prompted to reload the application
         * to ensure all plugin modules in use are up to date.
         */
        loadPlugin: (manifest: PluginManifest | string, forceReload?: boolean) => Promise<void>;
        /**
         * Enable the given plugin(s).
         *
         * Enabling a plugin puts all of its extensions into use.
         */
        enablePlugins: (pluginNames: string[]) => void;
        /**
         * Disable the given plugin(s) with an optional reason.
         *
         * Disabling a plugin puts all of its extensions out of use.
         */
        disablePlugins: (pluginNames: string[], disableReason?: string) => void;
        /**
         * Get a specific module exposed by the given plugin.
         */
        getExposedModule: <TModule extends AnyObject>(pluginName: string, moduleName: string) => Promise<TModule>;
    }`
  message: |-
    Type-alias 'PluginStoreInterface' had its type changed (type-alias): Return type of `PluginStoreInterface` changed from `{
        /**
         * Version of the `@openshift/dynamic-plugin-sdk` package.
         */
        readonly sdkVersion: string;
        /**
         * Subscribe to events emitted by the `PluginStore`.
         *
         * See {@link PluginEventType} for information on specific event types.
         *
         * Returns a function for unsubscribing the provided listener.
         */
        subscribe: (eventTypes: PluginEventType[], listener: VoidFunction) => VoidFunction;
        /**
         * Get extensions which are currently in use.
         *
         * An extension is in use when the associated plugin is currently enabled and its
         * feature flag requirements (if any) are met according to current feature flags.
         *
         * If you need to enhance or modify existing extension objects after the associated
         * plugin has been loaded and processed, we recommend using a custom React hook which
         * calls `useExtensions` or `useResolvedExtensions` and returns new extension object
         * instances.
         *
         * This method always returns a new array instance.
         */
        getExtensions: () => LoadedExtension[];
        /**
         * Get current information about plugins.
         *
         * This method always returns a new array instance.
         */
        getPluginInfo: () => PluginInfoEntry[];
        /**
         * Get current feature flags.
         *
         * This method always returns a new object.
         */
        getFeatureFlags: () => FeatureFlags;
        /**
         * Merge the entries of `newFlags` with current feature flags (non-boolean values
         * will be discarded).
         */
        setFeatureFlags: (newFlags: FeatureFlags) => void;
        /**
         * Start loading a plugin from the given manifest.
         *
         * The plugin manifest can be provided directly as an object or referenced via URL.
         *
         * Depending on the plugin's current load status, this method works as follows:
         * - plugin is loading - do nothing
         * - plugin has been loaded - reload only if `forceReload` is `true`
         * - plugin has failed to load - always reload
         *
         * The resulting Promise resolves when the load operation is complete. If the given
         * plugin is still loading when this method is invoked, the same Promise instance that
         * represents the load operation is returned.
         *
         * Use the `subscribe` method to respond to events emitted by the `PluginStore`.
         *
         * Be advised that any plugin modules which are already loaded by the host application
         * will _not_ be replaced upon reloading the associated plugin. This is due to webpack
         * module caching which also applies to federated modules. If a host application detects
         * changes in a plugin's deployment, users should be prompted to reload the application
         * to ensure all plugin modules in use are up to date.
         */
        loadPlugin: (manifest: PluginManifest | string, forceReload?: boolean) => Promise<void>;
        /**
         * Enable the given plugin(s).
         *
         * Enabling a plugin puts all of its extensions into use.
         */
        enablePlugins: (pluginNames: string[]) => void;
        /**
         * Disable the given plugin(s) with an optional reason.
         *
         * Disabling a plugin puts all of its extensions out of use.
         */
        disablePlugins: (pluginNames: string[], disableReason?: string) => void;
        /**
         * Get a specific module exposed by the given plugin.
         */
        getExposedModule: <TModule extends AnyObject>(pluginName: string, moduleName: string) => Promise<TModule>;
    }` to `{
        /**
         * Version of the `@openshift/dynamic-plugin-sdk` package.
         */
        readonly sdkVersion: string;
        /**
         * Subscribe to events emitted by the `PluginStore`.
         *
         * See {@link PluginEventType} for information on specific event types.
         *
         * Returns a function for unsubscribing the provided listener.
         */
        subscribe: (eventTypes: PluginEventType[], listener: VoidFunction) => VoidFunction;
        /**
         * Get extensions which are currently in use.
         *
         * An extension is in use when the associated plugin is currently enabled and its
         * feature flag requirements (if any) are met according to current feature flags.
         *
         * If you need to enhance or modify existing extension objects after the associated
         * plugin has been loaded and processed, we recommend using a custom React hook which
         * calls `useExtensions` or `useResolvedExtensions` and returns new extension object
         * instances.
         *
         * This method always returns a new array instance.
         */
        getExtensions: () => LoadedExtension[];
        /**
         * Get current information about plugins.
         *
         * This method always returns a new array instance.
         */
        getPluginInfo: () => PluginInfoEntry[];
        /**
         * Get current feature flags.
         *
         * This method always returns a new object.
         */
        getFeatureFlags: () => FeatureFlags;
        /**
         * Merge the entries of `newFlags` with current feature flags (non-boolean values
         * will be discarded).
         */
        setFeatureFlags: (newFlags: FeatureFlags) => void;
        /**
         * Start loading a plugin from the given manifest.
         *
         * The plugin manifest can be provided directly as an object or referenced via URL.
         *
         * Depending on the plugin's current load status, this method works as follows:
         * - plugin is loading - do nothing
         * - plugin has been loaded - reload only if `forceReload` is `true`
         * - plugin has failed to load - always reload
         *
         * The resulting Promise resolves when the load operation is complete. If the given
         * plugin is still loading when this method is invoked, the same Promise instance that
         * represents the load operation is returned.
         *
         * The resulting Promise rejects only when the plugin manifest cannot be loaded or
         * processed by the `PluginLoader` implementation.
         *
         * Use the `subscribe` method to respond to events emitted by the `PluginStore`.
         *
         * Be advised that any plugin modules which are already loaded by the host application
         * will _not_ be replaced upon reloading the associated plugin. This is due to webpack
         * module caching which also applies to federated modules. If a host application detects
         * changes in a plugin's deployment, users should be prompted to reload the application
         * to ensure all plugin modules in use are up to date.
         */
        loadPlugin: (manifest: PluginManifest | string, forceReload?: boolean) => Promise<void>;
        /**
         * Enable the given plugin(s).
         *
         * Enabling a plugin puts all of its extensions into use.
         */
        enablePlugins: (pluginNames: string[]) => void;
        /**
         * Disable the given plugin(s) with an optional reason.
         *
         * Disabling a plugin puts all of its extensions out of use.
         */
        disablePlugins: (pluginNames: string[], disableReason?: string) => void;
        /**
         * Get a specific module exposed by the given plugin.
         */
        getExposedModule: <TModule extends AnyObject>(pluginName: string, moduleName: string) => Promise<TModule>;
    }`

    File: packages/lib-core/src/types/lib-core/src/types/store.d.ts
    Before: {
        /**
         * Version of the `@openshift/dynamic-plugin-sdk` package.
         */
        readonly sdkVersion: string;
        /**
         * Subscribe to events emitted by the `PluginStore`.
         *
         * See {@link PluginEventType} for information on specific event types.
         *
         * Returns a function for unsubscribing the provided listener.
         */
        subscribe: (eventTypes: PluginEventType[], listener: VoidFunction) => VoidFunction;
        /**
         * Get extensions which are currently in use.
         *
         * An extension is in use when the associated plugin is currently enabled and its
         * feature flag requirements (if any) are met according to current feature flags.
         *
         * If you need to enhance or modify existing extension objects after the associated
         * plugin has been loaded and processed, we recommend using a custom React hook which
         * calls `useExtensions` or `useResolvedExtensions` and returns new extension object
         * instances.
         *
         * This method always returns a new array instance.
         */
        getExtensions: () => LoadedExtension[];
        /**
         * Get current information about plugins.
         *
         * This method always returns a new array instance.
         */
        getPluginInfo: () => PluginInfoEntry[];
        /**
         * Get current feature flags.
         *
         * This method always returns a new object.
         */
        getFeatureFlags: () => FeatureFlags;
        /**
         * Merge the entries of `newFlags` with current feature flags (non-boolean values
         * will be discarded).
         */
        setFeatureFlags: (newFlags: FeatureFlags) => void;
        /**
         * Start loading a plugin from the given manifest.
         *
         * The plugin manifest can be provided directly as an object or referenced via URL.
         *
         * Depending on the plugin's current load status, this method works as follows:
         * - plugin is loading - do nothing
         * - plugin has been loaded - reload only if `forceReload` is `true`
         * - plugin has failed to load - always reload
         *
         * The resulting Promise resolves when the load operation is complete. If the given
         * plugin is still loading when this method is invoked, the same Promise instance that
         * represents the load operation is returned.
         *
         * Use the `subscribe` method to respond to events emitted by the `PluginStore`.
         *
         * Be advised that any plugin modules which are already loaded by the host application
         * will _not_ be replaced upon reloading the associated plugin. This is due to webpack
         * module caching which also applies to federated modules. If a host application detects
         * changes in a plugin's deployment, users should be prompted to reload the application
         * to ensure all plugin modules in use are up to date.
         */
        loadPlugin: (manifest: PluginManifest | string, forceReload?: boolean) => Promise<void>;
        /**
         * Enable the given plugin(s).
         *
         * Enabling a plugin puts all of its extensions into use.
         */
        enablePlugins: (pluginNames: string[]) => void;
        /**
         * Disable the given plugin(s) with an optional reason.
         *
         * Disabling a plugin puts all of its extensions out of use.
         */
        disablePlugins: (pluginNames: string[], disableReason?: string) => void;
        /**
         * Get a specific module exposed by the given plugin.
         */
        getExposedModule: <TModule extends AnyObject>(pluginName: string, moduleName: string) => Promise<TModule>;
    }
    After: {
        /**
         * Version of the `@openshift/dynamic-plugin-sdk` package.
         */
        readonly sdkVersion: string;
        /**
         * Subscribe to events emitted by the `PluginStore`.
         *
         * See {@link PluginEventType} for information on specific event types.
         *
         * Returns a function for unsubscribing the provided listener.
         */
        subscribe: (eventTypes: PluginEventType[], listener: VoidFunction) => VoidFunction;
        /**
         * Get extensions which are currently in use.
         *
         * An extension is in use when the associated plugin is currently enabled and its
         * feature flag requirements (if any) are met according to current feature flags.
         *
         * If you need to enhance or modify existing extension objects after the associated
         * plugin has been loaded and processed, we recommend using a custom React hook which
         * calls `useExtensions` or `useResolvedExtensions` and returns new extension object
         * instances.
         *
         * This method always returns a new array instance.
         */
        getExtensions: () => LoadedExtension[];
        /**
         * Get current information about plugins.
         *
         * This method always returns a new array instance.
         */
        getPluginInfo: () => PluginInfoEntry[];
        /**
         * Get current feature flags.
         *
         * This method always returns a new object.
         */
        getFeatureFlags: () => FeatureFlags;
        /**
         * Merge the entries of `newFlags` with current feature flags (non-boolean values
         * will be discarded).
         */
        setFeatureFlags: (newFlags: FeatureFlags) => void;
        /**
         * Start loading a plugin from the given manifest.
         *
         * The plugin manifest can be provided directly as an object or referenced via URL.
         *
         * Depending on the plugin's current load status, this method works as follows:
         * - plugin is loading - do nothing
         * - plugin has been loaded - reload only if `forceReload` is `true`
         * - plugin has failed to load - always reload
         *
         * The resulting Promise resolves when the load operation is complete. If the given
         * plugin is still loading when this method is invoked, the same Promise instance that
         * represents the load operation is returned.
         *
         * The resulting Promise rejects only when the plugin manifest cannot be loaded or
         * processed by the `PluginLoader` implementation.
         *
         * Use the `subscribe` method to respond to events emitted by the `PluginStore`.
         *
         * Be advised that any plugin modules which are already loaded by the host application
         * will _not_ be replaced upon reloading the associated plugin. This is due to webpack
         * module caching which also applies to federated modules. If a host application detects
         * changes in a plugin's deployment, users should be prompted to reload the application
         * to ensure all plugin modules in use are up to date.
         */
        loadPlugin: (manifest: PluginManifest | string, forceReload?: boolean) => Promise<void>;
        /**
         * Enable the given plugin(s).
         *
         * Enabling a plugin puts all of its extensions into use.
         */
        enablePlugins: (pluginNames: string[]) => void;
        /**
         * Disable the given plugin(s) with an optional reason.
         *
         * Disabling a plugin puts all of its extensions out of use.
         */
        disablePlugins: (pluginNames: string[], disableReason?: string) => void;
        /**
         * Get a specific module exposed by the given plugin.
         */
        getExposedModule: <TModule extends AnyObject>(pluginName: string, moduleName: string) => Promise<TModule>;
    }
  when:
    or:
    - frontend.referenced:
        pattern: ^PluginStoreInterface$
        location: TYPE_REFERENCE
        from: '@openshift/dynamic-plugin-sdk'
    - frontend.referenced:
        pattern: ^PluginStoreInterface$
        location: IMPORT
        from: '@openshift/dynamic-plugin-sdk'
- ruleID: semver-packages-lib-utils-src-index-d-ts-detailsitem-removed-group-4
  labels:
  - change-type=removed
  - has-codemod=false
  - kind=constant
  - package=@openshift/dynamic-plugin-sdk-utils
  - source=semver-analyzer
  effort: 5
  category: mandatory
  description: 4 related changes
  message: |
    This rule contains 4 migration steps. Apply each one independently:

    Step 1 of 4:
    Constant 'DetailsItem' was removed (constant): Exported constant `DetailsItem` was removed

    File: packages/lib-utils/src/index.d.ts
    Before: constant: DetailsItem: React_3.FC<DetailsItemProps>

    Step 2 of 4:
    Constant 'GettingStartedCard' was removed (constant): Exported constant `GettingStartedCard` was removed

    File: packages/lib-utils/src/index.d.ts
    Before: constant: GettingStartedCard: FC<GettingStartedCardProps>

    Step 3 of 4:
    Constant 'ListView' was removed (constant): Exported constant `ListView` was removed

    File: packages/lib-utils/src/index.d.ts
    Before: constant: ListView: <D extends AnyObject>(<destructured>: ListViewProps<D>) => JSX.Element

    Step 4 of 4:
    Constant 'ResourceSummary' was removed (constant): Exported constant `ResourceSummary` was removed

    File: packages/lib-utils/src/index.d.ts
    Before: constant: ResourceSummary: React_3.FC<ResourceSummaryProps>
  when:
    or:
    - or:
      - frontend.referenced:
          pattern: ^DetailsItem$
          location: JSX_COMPONENT
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^DetailsItem$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
    - or:
      - frontend.referenced:
          pattern: ^GettingStartedCard$
          location: JSX_COMPONENT
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^GettingStartedCard$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
    - or:
      - frontend.referenced:
          pattern: ^ListView$
          location: JSX_COMPONENT
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^ListView$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
    - or:
      - frontend.referenced:
          pattern: ^ResourceSummary$
          location: JSX_COMPONENT
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^ResourceSummary$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
- ruleID: semver-packages-lib-utils-src-index-d-ts-detailsitemprops-removed-group-3
  labels:
  - change-type=removed
  - has-codemod=false
  - kind=type-alias
  - package=@openshift/dynamic-plugin-sdk-utils
  - source=semver-analyzer
  effort: 5
  category: mandatory
  description: 3 related changes
  message: |
    This rule contains 3 migration steps. Apply each one independently:

    Step 1 of 3:
    Type-alias 'DetailsItemProps' was removed (type-alias): Exported type alias `DetailsItemProps` was removed

    File: packages/lib-utils/src/index.d.ts
    Before: type alias: DetailsItemProps: {
        defaultValue?: string;
        hideEmpty?: boolean;
        loaded?: boolean;
        label: string;
        resource?: K8sResourceCommon;
        path?: string | string[];
    }

    Step 2 of 3:
    Type-alias 'OverViewPageProps' was removed (type-alias): Exported type alias `OverViewPageProps` was removed

    File: packages/lib-utils/src/index.d.ts
    Before: type alias: OverViewPageProps: {
        loaded?: boolean;
        loadError?: LoadError;
        resource: K8sResourceCommon;
        rightColumn?: ReactElement;
        additionalErrorDescription?: string;
    }

    Step 3 of 3:
    Type-alias 'ResourceSummaryProps' was removed (type-alias): Exported type alias `ResourceSummaryProps` was removed

    File: packages/lib-utils/src/index.d.ts
    Before: type alias: ResourceSummaryProps: {
        resource?: K8sResourceCommon;
        loaded?: boolean;
    }
  when:
    or:
    - or:
      - frontend.referenced:
          pattern: ^DetailsItemProps$
          location: TYPE_REFERENCE
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^DetailsItemProps$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
    - or:
      - frontend.referenced:
          pattern: ^OverViewPageProps$
          location: TYPE_REFERENCE
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^OverViewPageProps$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
    - or:
      - frontend.referenced:
          pattern: ^ResourceSummaryProps$
          location: TYPE_REFERENCE
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^ResourceSummaryProps$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
- ruleID: semver-packages-lib-utils-src-types-components-details-page-header-detailspageheader-d-ts-detailspageheader-removed
  labels:
  - source=semver-analyzer
  - change-type=removed
  - kind=constant
  - has-codemod=false
  - package=@openshift/dynamic-plugin-sdk-utils
  effort: 5
  category: mandatory
  description: Exported constant `DetailsPageHeader` was removed
  message: |-
    Constant 'DetailsPageHeader' was removed (constant): Exported constant `DetailsPageHeader` was removed

    File: packages/lib-utils/src/types/components/details-page-header/DetailsPageHeader.d.ts
    Before: constant: DetailsPageHeader: SFC<DetailsPageHeaderProps>
  when:
    or:
    - frontend.referenced:
        pattern: ^DetailsPageHeader$
        location: JSX_COMPONENT
        from: '@openshift/dynamic-plugin-sdk-utils'
    - frontend.referenced:
        pattern: ^DetailsPageHeader$
        location: IMPORT
        from: '@openshift/dynamic-plugin-sdk-utils'
- ruleID: semver-packages-lib-utils-src-types-components-details-page-header-detailspageheader-d-ts-detailspageheaderprops-removed-group-2
  labels:
  - change-type=removed
  - has-codemod=false
  - kind=type-alias
  - package=@openshift/dynamic-plugin-sdk-utils
  - source=semver-analyzer
  effort: 5
  category: mandatory
  description: 2 related changes
  message: |
    This rule contains 2 migration steps. Apply each one independently:

    Step 1 of 2:
    Type-alias 'DetailsPageHeaderProps' was removed (type-alias): Exported type alias `DetailsPageHeaderProps` was removed

    File: packages/lib-utils/src/types/components/details-page-header/DetailsPageHeader.d.ts
    Before: type alias: DetailsPageHeaderProps: {
        breadcrumbs: BreadcrumbProp[];
        actionButtons?: ActionButtonProp[];
        pageHeading?: PageHeading;
        /** Optional resource object (if no title for the page heading is provided, the title can be taken from the resource's name) */
        obj?: K8sResourceCommon;
        actionMenu?: ActionMenuProps;
    }

    Step 2 of 2:
    Type-alias 'PageHeading' was removed (type-alias): Exported type alias `PageHeading` was removed

    File: packages/lib-utils/src/types/components/details-page-header/DetailsPageHeader.d.ts
    Before: type alias: PageHeading: {
        /** Optional title for page heading */
        title?: string;
        /** Optional label for page heading */
        label?: {
            name: string;
            key?: string;
            icon?: React.ReactNode;
            href?: string;
            color?: 'blue' | 'cyan' | 'green' | 'orange' | 'purple' | 'red' | 'grey';
        };
        /** Optional icon for page heading (appears to the left of the page heading's title) */
        iconBeforeTitle?: React.ReactNode;
        /** Optional icon for page heading (appears to the right of the page heading's title) */
        iconAfterTitle?: React.ReactNode;
    }
  when:
    or:
    - or:
      - frontend.referenced:
          pattern: ^DetailsPageHeaderProps$
          location: TYPE_REFERENCE
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^DetailsPageHeaderProps$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
    - or:
      - frontend.referenced:
          pattern: ^PageHeading$
          location: TYPE_REFERENCE
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^PageHeading$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
- ruleID: semver-packages-lib-utils-src-types-components-details-page-header-utils-actionbuttons-d-ts-actionbuttons-removed
  labels:
  - source=semver-analyzer
  - change-type=removed
  - kind=constant
  - has-codemod=false
  - package=@openshift/dynamic-plugin-sdk-utils
  effort: 5
  category: mandatory
  description: Exported constant `ActionButtons` was removed
  message: |-
    Constant 'ActionButtons' was removed (constant): Exported constant `ActionButtons` was removed

    File: packages/lib-utils/src/types/components/details-page-header/utils/ActionButtons.d.ts
    Before: constant: ActionButtons: SFC<ActionButtonsProps>
  when:
    or:
    - frontend.referenced:
        pattern: ^ActionButtons$
        location: JSX_COMPONENT
        from: '@openshift/dynamic-plugin-sdk-utils'
    - frontend.referenced:
        pattern: ^ActionButtons$
        location: IMPORT
        from: '@openshift/dynamic-plugin-sdk-utils'
- ruleID: semver-packages-lib-utils-src-types-components-details-page-header-utils-actionbuttons-d-ts-actionbuttonprop-removed-group-2
  labels:
  - change-type=removed
  - has-codemod=false
  - kind=type-alias
  - package=@openshift/dynamic-plugin-sdk-utils
  - source=semver-analyzer
  effort: 5
  category: mandatory
  description: 2 related changes
  message: |
    This rule contains 2 migration steps. Apply each one independently:

    Step 1 of 2:
    Type-alias 'ActionButtonProp' was removed (type-alias): Exported type alias `ActionButtonProp` was removed

    File: packages/lib-utils/src/types/components/details-page-header/utils/ActionButtons.d.ts
    Before: type alias: ActionButtonProp: {
        id?: string;
        label?: string;
        callback: (event: React.MouseEvent) => void;
        isDisabled?: boolean;
        tooltip?: string;
        variant?: 'primary' | 'secondary' | 'tertiary' | 'link';
    }

    Step 2 of 2:
    Type-alias 'ActionButtonsProps' was removed (type-alias): Exported type alias `ActionButtonsProps` was removed

    File: packages/lib-utils/src/types/components/details-page-header/utils/ActionButtons.d.ts
    Before: type alias: ActionButtonsProps: {
        actionButtons: ActionButtonProp[];
    }
  when:
    or:
    - or:
      - frontend.referenced:
          pattern: ^ActionButtonProp$
          location: TYPE_REFERENCE
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^ActionButtonProp$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
    - or:
      - frontend.referenced:
          pattern: ^ActionButtonsProps$
          location: TYPE_REFERENCE
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^ActionButtonsProps$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
- ruleID: semver-packages-lib-utils-src-types-components-details-page-header-utils-actionmenu-d-ts-actionmenu-removed
  labels:
  - source=semver-analyzer
  - change-type=removed
  - kind=constant
  - has-codemod=false
  - package=@openshift/dynamic-plugin-sdk-utils
  effort: 5
  category: mandatory
  description: Exported constant `ActionMenu` was removed
  message: |-
    Constant 'ActionMenu' was removed (constant): Exported constant `ActionMenu` was removed

    File: packages/lib-utils/src/types/components/details-page-header/utils/ActionMenu.d.ts
    Before: constant: ActionMenu: FC<ActionMenuProps>
  when:
    or:
    - frontend.referenced:
        pattern: ^ActionMenu$
        location: JSX_COMPONENT
        from: '@openshift/dynamic-plugin-sdk-utils'
    - frontend.referenced:
        pattern: ^ActionMenu$
        location: IMPORT
        from: '@openshift/dynamic-plugin-sdk-utils'
- ruleID: semver-packages-lib-utils-src-types-components-details-page-header-utils-actionmenu-d-ts-action-removed-group-6
  labels:
  - change-type=removed
  - has-codemod=false
  - kind=type-alias
  - package=@openshift/dynamic-plugin-sdk-utils
  - source=semver-analyzer
  effort: 5
  category: mandatory
  description: 6 related changes
  message: |
    This rule contains 6 migration steps. Apply each one independently:

    Step 1 of 6:
    Type-alias 'Action' was removed (type-alias): Exported type alias `Action` was removed

    File: packages/lib-utils/src/types/components/details-page-header/utils/ActionMenu.d.ts
    Before: type alias: Action: {
        /** A unique identifier for this action. */
        id: string;
        /** The label to display in the UI. */
        label: React.ReactNode;
        /** Optional subtext for the menu item */
        description?: string;
        /** Executable callback or href. External links should automatically provide an external link icon on action. */
        cta: ActionCTA;
        /** Optional flag to indicate whether the action is disabled. */
        isDisabled?: boolean;
        /** Optional tooltip for this action. */
        tooltip?: string;
        /** Optional icon for this action. */
        icon?: React.ReactNode;
    }

    Step 2 of 6:
    Type-alias 'ActionCTA' was removed (type-alias): Exported type alias `ActionCTA` was removed

    File: packages/lib-utils/src/types/components/details-page-header/utils/ActionMenu.d.ts
    Before: type alias: ActionCTA: {
        callback: (event: React.MouseEvent<any> | React.KeyboardEvent | MouseEvent) => void;
    } | {
        href: string;
        external?: boolean;
    }

    Step 3 of 6:
    Type-alias 'ActionMenuOptions' was removed (type-alias): Exported type alias `ActionMenuOptions` was removed

    File: packages/lib-utils/src/types/components/details-page-header/utils/ActionMenu.d.ts
    Before: type alias: ActionMenuOptions: {
        /** Optional flag to indicate whether action menu should be disabled */
        isDisabled?: boolean;
        /** Optional variant for action menu: DROPDOWN vs KEBAB (defaults to dropdown) */
        variant?: ActionMenuVariant;
        /** Optional label for action menu (defaults to 'Actions') */
        label?: string;
        /** Optional position (left/right) at which the action menu appears (defaults to right) */
        position?: DropdownPosition;
        /** Optional flag to indicate whether labels should appear to the left of icons for the action menu items (icon appears after the label by default) */
        displayLabelBeforeIcon?: boolean;
    }

    Step 4 of 6:
    Type-alias 'ActionMenuProps' was removed (type-alias): Exported type alias `ActionMenuProps` was removed

    File: packages/lib-utils/src/types/components/details-page-header/utils/ActionMenu.d.ts
    Before: type alias: ActionMenuProps: EitherNotBoth<{
        actions: Action[];
    }, {
        groupedActions: GroupedActions[];
    }> & ActionMenuOptions

    Step 5 of 6:
    Type-alias 'ActionMenuVariant' was removed (type-alias): Exported enum `ActionMenuVariant` was removed

    File: packages/lib-utils/src/types/components/details-page-header/utils/ActionMenu.d.ts
    Before: enum: ActionMenuVariant

    Step 6 of 6:
    Type-alias 'GroupedActions' was removed (type-alias): Exported type alias `GroupedActions` was removed

    File: packages/lib-utils/src/types/components/details-page-header/utils/ActionMenu.d.ts
    Before: type alias: GroupedActions: {
        /** A unique identifier for this group. */
        groupId: string;
        /** Optional label to display as group heading */
        groupLabel?: string;
        /** Actions under this group. */
        groupActions: Action[];
    }
  when:
    or:
    - or:
      - frontend.referenced:
          pattern: ^Action$
          location: TYPE_REFERENCE
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^Action$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
    - or:
      - frontend.referenced:
          pattern: ^ActionCTA$
          location: TYPE_REFERENCE
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^ActionCTA$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
    - or:
      - frontend.referenced:
          pattern: ^ActionMenuOptions$
          location: TYPE_REFERENCE
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^ActionMenuOptions$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
    - or:
      - frontend.referenced:
          pattern: ^ActionMenuProps$
          location: TYPE_REFERENCE
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^ActionMenuProps$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
    - or:
      - frontend.referenced:
          pattern: ^ActionMenuVariant$
          location: TYPE_REFERENCE
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^ActionMenuVariant$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
    - or:
      - frontend.referenced:
          pattern: ^GroupedActions$
          location: TYPE_REFERENCE
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^GroupedActions$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
- ruleID: semver-packages-lib-utils-src-types-components-details-page-header-utils-breadcrumbs-d-ts-breadcrumbs-removed
  labels:
  - source=semver-analyzer
  - change-type=removed
  - kind=constant
  - has-codemod=false
  - package=@openshift/dynamic-plugin-sdk-utils
  effort: 5
  category: mandatory
  description: Exported constant `Breadcrumbs` was removed
  message: |-
    Constant 'Breadcrumbs' was removed (constant): Exported constant `Breadcrumbs` was removed

    File: packages/lib-utils/src/types/components/details-page-header/utils/Breadcrumbs.d.ts
    Before: constant: Breadcrumbs: SFC<BreadcrumbsProps>
  when:
    or:
    - frontend.referenced:
        pattern: ^Breadcrumbs$
        location: JSX_COMPONENT
        from: '@openshift/dynamic-plugin-sdk-utils'
    - frontend.referenced:
        pattern: ^Breadcrumbs$
        location: IMPORT
        from: '@openshift/dynamic-plugin-sdk-utils'
- ruleID: semver-packages-lib-utils-src-types-components-details-page-header-utils-breadcrumbs-d-ts-breadcrumbprop-removed-group-2
  labels:
  - change-type=removed
  - has-codemod=false
  - kind=type-alias
  - package=@openshift/dynamic-plugin-sdk-utils
  - source=semver-analyzer
  effort: 5
  category: mandatory
  description: 2 related changes
  message: |
    This rule contains 2 migration steps. Apply each one independently:

    Step 1 of 2:
    Type-alias 'BreadcrumbProp' was removed (type-alias): Exported type alias `BreadcrumbProp` was removed

    File: packages/lib-utils/src/types/components/details-page-header/utils/Breadcrumbs.d.ts
    Before: type alias: BreadcrumbProp: {
        name: string;
        path: string;
        uuid?: string;
        id?: string;
    }

    Step 2 of 2:
    Type-alias 'BreadcrumbsProps' was removed (type-alias): Exported type alias `BreadcrumbsProps` was removed

    File: packages/lib-utils/src/types/components/details-page-header/utils/Breadcrumbs.d.ts
    Before: type alias: BreadcrumbsProps: {
        breadcrumbs: BreadcrumbProp[];
    }
  when:
    or:
    - or:
      - frontend.referenced:
          pattern: ^BreadcrumbProp$
          location: TYPE_REFERENCE
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^BreadcrumbProp$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
    - or:
      - frontend.referenced:
          pattern: ^BreadcrumbsProps$
          location: TYPE_REFERENCE
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^BreadcrumbsProps$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
- ruleID: semver-packages-lib-utils-src-types-components-details-page-detailspage-d-ts-detailspage-removed
  labels:
  - source=semver-analyzer
  - change-type=removed
  - kind=constant
  - has-codemod=false
  - package=@openshift/dynamic-plugin-sdk-utils
  effort: 5
  category: mandatory
  description: Exported constant `DetailsPage` was removed
  message: |-
    Constant 'DetailsPage' was removed (constant): Exported constant `DetailsPage` was removed

    File: packages/lib-utils/src/types/components/details-page/DetailsPage.d.ts
    Before: constant: DetailsPage: (props: Omit<DetailsPageProps, keyof WithRouterProps>) => JSX.Element
  when:
    or:
    - frontend.referenced:
        pattern: ^DetailsPage$
        location: JSX_COMPONENT
        from: '@openshift/dynamic-plugin-sdk-utils'
    - frontend.referenced:
        pattern: ^DetailsPage$
        location: IMPORT
        from: '@openshift/dynamic-plugin-sdk-utils'
- ruleID: semver-packages-lib-utils-src-types-components-details-page-detailspage-d-ts-detailspageprops-removed
  labels:
  - source=semver-analyzer
  - change-type=removed
  - kind=type-alias
  - has-codemod=false
  - package=@openshift/dynamic-plugin-sdk-utils
  effort: 5
  category: mandatory
  description: Exported type alias `DetailsPageProps` was removed
  message: |-
    Type-alias 'DetailsPageProps' was removed (type-alias): Exported type alias `DetailsPageProps` was removed

    File: packages/lib-utils/src/types/components/details-page/DetailsPage.d.ts
    Before: type alias: DetailsPageProps: HorizontalNavProps & DetailsPageHeaderProps
  when:
    or:
    - frontend.referenced:
        pattern: ^DetailsPageProps$
        location: TYPE_REFERENCE
        from: '@openshift/dynamic-plugin-sdk-utils'
    - frontend.referenced:
        pattern: ^DetailsPageProps$
        location: IMPORT
        from: '@openshift/dynamic-plugin-sdk-utils'
- ruleID: semver-packages-lib-utils-src-types-components-getting-started-card-gettingstartedcard-d-ts-gettingstartedcardprops-removed
  labels:
  - source=semver-analyzer
  - change-type=removed
  - kind=type-alias
  - has-codemod=false
  - package=@openshift/dynamic-plugin-sdk-utils
  effort: 5
  category: mandatory
  description: Exported type alias `GettingStartedCardProps` was removed
  message: |-
    Type-alias 'GettingStartedCardProps' was removed (type-alias): Exported type alias `GettingStartedCardProps` was removed

    File: packages/lib-utils/src/types/components/getting-started-card/GettingStartedCard.d.ts
    Before: type alias: GettingStartedCardProps: {
        cardClassName?: string;
        imgAlt?: string;
        imgClassName?: string;
        imgSrc?: string;
        isDismissable?: boolean;
        localStorageKey: string;
        title: string;
    }
  when:
    or:
    - frontend.referenced:
        pattern: ^GettingStartedCardProps$
        location: TYPE_REFERENCE
        from: '@openshift/dynamic-plugin-sdk-utils'
    - frontend.referenced:
        pattern: ^GettingStartedCardProps$
        location: IMPORT
        from: '@openshift/dynamic-plugin-sdk-utils'
- ruleID: semver-packages-lib-utils-src-types-components-getting-started-card-index-d-ts-gettingstartedcard-removed
  labels:
  - source=semver-analyzer
  - change-type=removed
  - kind=constant
  - has-codemod=false
  - package=@openshift/dynamic-plugin-sdk-utils
  effort: 5
  category: mandatory
  description: Exported variable `GettingStartedCard` was removed
  message: |-
    Constant 'GettingStartedCard' was removed (constant): Exported variable `GettingStartedCard` was removed

    File: packages/lib-utils/src/types/components/getting-started-card/index.d.ts
    Before: variable: GettingStartedCard
  when:
    or:
    - frontend.referenced:
        pattern: ^GettingStartedCard$
        location: JSX_COMPONENT
        from: '@openshift/dynamic-plugin-sdk-utils'
    - frontend.referenced:
        pattern: ^GettingStartedCard$
        location: IMPORT
        from: '@openshift/dynamic-plugin-sdk-utils'
- ruleID: semver-packages-lib-utils-src-types-components-horizontal-nav-horizontalnav-d-ts-horizontalnav-removed
  labels:
  - source=semver-analyzer
  - change-type=removed
  - kind=constant
  - has-codemod=false
  - package=@openshift/dynamic-plugin-sdk-utils
  effort: 5
  category: mandatory
  description: Exported variable `HorizontalNav` was removed
  message: |-
    Constant 'HorizontalNav' was removed (constant): Exported variable `HorizontalNav` was removed

    File: packages/lib-utils/src/types/components/horizontal-nav/HorizontalNav.d.ts
    Before: variable: HorizontalNav
  when:
    or:
    - frontend.referenced:
        pattern: ^HorizontalNav$
        location: JSX_COMPONENT
        from: '@openshift/dynamic-plugin-sdk-utils'
    - frontend.referenced:
        pattern: ^HorizontalNav$
        location: IMPORT
        from: '@openshift/dynamic-plugin-sdk-utils'
- ruleID: semver-packages-lib-utils-src-types-components-horizontal-nav-horizontalnav-d-ts-withrouterprops-removed
  labels:
  - source=semver-analyzer
  - change-type=removed
  - kind=type-alias
  - has-codemod=false
  - package=@openshift/dynamic-plugin-sdk-utils
  effort: 5
  category: mandatory
  description: Exported type alias `WithRouterProps` was removed
  message: |-
    Type-alias 'WithRouterProps' was removed (type-alias): Exported type alias `WithRouterProps` was removed

    File: packages/lib-utils/src/types/components/horizontal-nav/HorizontalNav.d.ts
    Before: type alias: WithRouterProps: {
        /** URL parameters */
        params?: Record<string, string>;
        /** Navigate function */
        navigate?: ReturnType<typeof useNavigate>;
        /** Current location */
        location?: ReturnType<typeof useLocation>;
    }
  when:
    or:
    - frontend.referenced:
        pattern: ^WithRouterProps$
        location: TYPE_REFERENCE
        from: '@openshift/dynamic-plugin-sdk-utils'
    - frontend.referenced:
        pattern: ^WithRouterProps$
        location: IMPORT
        from: '@openshift/dynamic-plugin-sdk-utils'
- ruleID: semver-packages-lib-utils-src-types-components-label-list-labellist-d-ts-labellist-removed
  labels:
  - source=semver-analyzer
  - change-type=removed
  - kind=constant
  - has-codemod=false
  - package=@openshift/dynamic-plugin-sdk-utils
  effort: 5
  category: mandatory
  description: Exported constant `LabelList` was removed
  message: |-
    Constant 'LabelList' was removed (constant): Exported constant `LabelList` was removed

    File: packages/lib-utils/src/types/components/label-list/LabelList.d.ts
    Before: constant: LabelList: FC<LabelListProps>
  when:
    or:
    - frontend.referenced:
        pattern: ^LabelList$
        location: JSX_COMPONENT
        from: '@openshift/dynamic-plugin-sdk-utils'
    - frontend.referenced:
        pattern: ^LabelList$
        location: IMPORT
        from: '@openshift/dynamic-plugin-sdk-utils'
- ruleID: semver-packages-lib-utils-src-types-components-label-list-labellist-d-ts-hrefforlabels-removed-group-2
  labels:
  - change-type=removed
  - has-codemod=false
  - kind=type-alias
  - package=@openshift/dynamic-plugin-sdk-utils
  - source=semver-analyzer
  effort: 5
  category: mandatory
  description: 2 related changes
  message: |
    This rule contains 2 migration steps. Apply each one independently:

    Step 1 of 2:
    Type-alias 'HrefForLabels' was removed (type-alias): Exported type alias `HrefForLabels` was removed

    File: packages/lib-utils/src/types/components/label-list/LabelList.d.ts
    Before: type alias: HrefForLabels: {
        [key: string]: string;
    }

    Step 2 of 2:
    Type-alias 'LabelListProps' was removed (type-alias): Exported type alias `LabelListProps` was removed

    File: packages/lib-utils/src/types/components/label-list/LabelList.d.ts
    Before: type alias: LabelListProps: {
        /** Key value pair for labels (k8s labels) */
        labels?: Record<string, string>;
        /** Optional icon for labels in list */
        icon?: React.ReactNode;
        /** Optional icon for labels in list */
        color?: 'blue' | 'cyan' | 'green' | 'orange' | 'purple' | 'red' | 'grey';
        /** Optional href for each label */
        hrefForLabels?: HrefForLabels;
        /** Optional flag indicating that the label list appears expanded by default */
        expand?: boolean;
    }
  when:
    or:
    - or:
      - frontend.referenced:
          pattern: ^HrefForLabels$
          location: TYPE_REFERENCE
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^HrefForLabels$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
    - or:
      - frontend.referenced:
          pattern: ^LabelListProps$
          location: TYPE_REFERENCE
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^LabelListProps$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
- ruleID: semver-packages-lib-utils-src-types-components-list-view-listview-d-ts-filterdefault-removed
  labels:
  - source=semver-analyzer
  - change-type=removed
  - kind=function
  - has-codemod=false
  - package=@openshift/dynamic-plugin-sdk-utils
  effort: 5
  category: mandatory
  description: Exported function `filterDefault` was removed
  message: |-
    Function 'filterDefault' was removed (function): Exported function `filterDefault` was removed

    File: packages/lib-utils/src/types/components/list-view/ListView.d.ts
    Before: function: filterDefault: D[]
  when:
    frontend.referenced:
      pattern: ^filterDefault$
      location: FUNCTION_CALL
      from: '@openshift/dynamic-plugin-sdk-utils'
- ruleID: semver-packages-lib-utils-src-types-components-list-view-listview-d-ts-filteritem-removed-group-2
  labels:
  - change-type=removed
  - has-codemod=false
  - kind=type-alias
  - package=@openshift/dynamic-plugin-sdk-utils
  - source=semver-analyzer
  effort: 5
  category: mandatory
  description: 2 related changes
  message: |
    This rule contains 2 migration steps. Apply each one independently:

    Step 1 of 2:
    Type-alias 'FilterItem' was removed (type-alias): Exported type alias `FilterItem` was removed

    File: packages/lib-utils/src/types/components/list-view/ListView.d.ts
    Before: type alias: FilterItem: {
        /** Label of a parameter used for filtering. */
        label: string;
        /** Column name for given filtering parameter. */
        id: string;
    }

    Step 2 of 2:
    Type-alias 'ListViewProps' was removed (type-alias): Exported type alias `ListViewProps` was removed

    File: packages/lib-utils/src/types/components/list-view/ListView.d.ts
    Before: type alias: ListViewProps: VirtualizedTableProps<D> & {
        /** Optional custom onFilter callback. */
        onFilter?: (filterValues: Record<string, string[]>, activeFilter?: FilterItem) => D[];
        /** Optional array of filterBy options. */
        filters?: FilterItem[];
        actionButtons?: ActionButtonProp[];
    }
  when:
    or:
    - or:
      - frontend.referenced:
          pattern: ^FilterItem$
          location: TYPE_REFERENCE
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^FilterItem$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
    - or:
      - frontend.referenced:
          pattern: ^ListViewProps$
          location: TYPE_REFERENCE
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^ListViewProps$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
- ruleID: semver-packages-lib-utils-src-types-components-list-view-index-d-ts-listview-removed
  labels:
  - source=semver-analyzer
  - change-type=removed
  - kind=constant
  - has-codemod=false
  - package=@openshift/dynamic-plugin-sdk-utils
  effort: 5
  category: mandatory
  description: Exported variable `ListView` was removed
  message: |-
    Constant 'ListView' was removed (constant): Exported variable `ListView` was removed

    File: packages/lib-utils/src/types/components/list-view/index.d.ts
    Before: variable: ListView
  when:
    or:
    - frontend.referenced:
        pattern: ^ListView$
        location: JSX_COMPONENT
        from: '@openshift/dynamic-plugin-sdk-utils'
    - frontend.referenced:
        pattern: ^ListView$
        location: IMPORT
        from: '@openshift/dynamic-plugin-sdk-utils'
- ruleID: semver-packages-lib-utils-src-types-components-resource-summary-index-d-ts-detailsitem-removed-group-2
  labels:
  - change-type=removed
  - has-codemod=false
  - kind=constant
  - package=@openshift/dynamic-plugin-sdk-utils
  - source=semver-analyzer
  effort: 5
  category: mandatory
  description: 2 related changes
  message: |
    This rule contains 2 migration steps. Apply each one independently:

    Step 1 of 2:
    Constant 'DetailsItem' was removed (constant): Exported variable `DetailsItem` was removed

    File: packages/lib-utils/src/types/components/resource-summary/index.d.ts
    Before: variable: DetailsItem

    Step 2 of 2:
    Constant 'ResourceSummary' was removed (constant): Exported variable `ResourceSummary` was removed

    File: packages/lib-utils/src/types/components/resource-summary/index.d.ts
    Before: variable: ResourceSummary
  when:
    or:
    - or:
      - frontend.referenced:
          pattern: ^DetailsItem$
          location: JSX_COMPONENT
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^DetailsItem$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
    - or:
      - frontend.referenced:
          pattern: ^ResourceSummary$
          location: JSX_COMPONENT
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^ResourceSummary$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
- ruleID: semver-packages-lib-utils-src-types-components-status-statusbox-d-ts-statusbox-removed
  labels:
  - source=semver-analyzer
  - change-type=removed
  - kind=constant
  - has-codemod=false
  - package=@openshift/dynamic-plugin-sdk-utils
  effort: 5
  category: mandatory
  description: Exported constant `StatusBox` was removed
  message: |-
    Constant 'StatusBox' was removed (constant): Exported constant `StatusBox` was removed

    File: packages/lib-utils/src/types/components/status/StatusBox.d.ts
    Before: constant: StatusBox: FC<StatusBoxProps>
  when:
    or:
    - frontend.referenced:
        pattern: ^StatusBox$
        location: JSX_COMPONENT
        from: '@openshift/dynamic-plugin-sdk-utils'
    - frontend.referenced:
        pattern: ^StatusBox$
        location: IMPORT
        from: '@openshift/dynamic-plugin-sdk-utils'
- ruleID: semver-packages-lib-utils-src-types-components-status-statusbox-d-ts-loaderror-removed-group-2
  labels:
  - change-type=removed
  - has-codemod=false
  - kind=type-alias
  - package=@openshift/dynamic-plugin-sdk-utils
  - source=semver-analyzer
  effort: 5
  category: mandatory
  description: 2 related changes
  message: |
    This rule contains 2 migration steps. Apply each one independently:

    Step 1 of 2:
    Type-alias 'LoadError' was removed (type-alias): Exported type alias `LoadError` was removed

    File: packages/lib-utils/src/types/components/status/StatusBox.d.ts
    Before: type alias: LoadError: {
        /** Error message. */
        message: string;
        /** Error HTTP status code. */
        status: number;
    }

    Step 2 of 2:
    Type-alias 'StatusBoxProps' was removed (type-alias): Exported type alias `StatusBoxProps` was removed

    File: packages/lib-utils/src/types/components/status/StatusBox.d.ts
    Before: type alias: StatusBoxProps: {
        /** Optional flag indicating that filters are applied to data */
        areFiltersApplied?: boolean;
        /** Flag indicating that no data exist */
        noData: boolean;
        /** Optional children element to be rendered */
        children?: React.ReactElement;
        /** Optional empty state description string */
        emptyStateDescription?: string;
        /** Optional load error object */
        loadError?: LoadError;
        /** Optional flag indicating that data has been loaded */
        loaded?: boolean;
        /** Custom empty state when no data exist */
        CustomNoDataEmptyState?: React.ReactElement;
        /** Custom empty state when there are no applicable data */
        CustomEmptyState?: React.ReactElement;
    }
  when:
    or:
    - or:
      - frontend.referenced:
          pattern: ^LoadError$
          location: TYPE_REFERENCE
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^LoadError$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
    - or:
      - frontend.referenced:
          pattern: ^StatusBoxProps$
          location: TYPE_REFERENCE
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^StatusBoxProps$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
- ruleID: semver-packages-lib-utils-src-types-components-table-virtualizedtable-d-ts-tablepagination-removed-group-2
  labels:
  - change-type=removed
  - has-codemod=false
  - kind=type-alias
  - package=@openshift/dynamic-plugin-sdk-utils
  - source=semver-analyzer
  effort: 5
  category: mandatory
  description: 2 related changes
  message: |
    This rule contains 2 migration steps. Apply each one independently:

    Step 1 of 2:
    Type-alias 'TablePagination' was removed (type-alias): Exported type alias `TablePagination` was removed

    File: packages/lib-utils/src/types/components/table/VirtualizedTable.d.ts
    Before: type alias: TablePagination: {
        limit: number;
        offset: number;
    }

    Step 2 of 2:
    Type-alias 'VirtualizedTableProps' was removed (type-alias): Exported type alias `VirtualizedTableProps` was removed

    File: packages/lib-utils/src/types/components/table/VirtualizedTable.d.ts
    Before: type alias: VirtualizedTableProps: {
        /** Optional flag indicating that filters are applied to data. */
        areFiltersApplied?: boolean;
        /** Optional actions for each row. */
        rowActions?: IAction[];
        /** Data array. */
        data: D[];
        /** Flag indicating data has been loaded. */
        loaded: boolean;
        /** Optional load error object. */
        loadError?: LoadError;
        /** Table columns array. */
        columns: TableColumn<D>[];
        /** Table row component. */
        Row: React.ComponentType<RowProps<D>>;
        /** DEPRECATED - Optional load error default text. No longer takes effect and will be removed */
        loadErrorDefaultText?: string;
        /** Optional isSelected row callback */
        isRowSelected?: (item: D) => boolean;
        /** Optional onSelect row callback */
        onSelect?: (event: React.FormEvent<HTMLInputElement>, isRowSelected: boolean, data: D[]) => void;
        /** Optional pagination params */
        pagination?: TablePagination;
        /** Optional no data empty state component. */
        CustomNoDataEmptyState?: React.ReactElement;
        /** Optional no applicable data empty state component. */
        CustomEmptyState?: React.ReactElement;
        /** Optional empty state description. */
        emptyStateDescription?: string;
        /** Optional aria label. */
        'aria-label'?: string;
        /** Optional scroll node. */
        scrollNode?: HTMLElement;
        /** Optional virtualized table flag */
        virtualized?: boolean;
    }
  when:
    or:
    - or:
      - frontend.referenced:
          pattern: ^TablePagination$
          location: TYPE_REFERENCE
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^TablePagination$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
    - or:
      - frontend.referenced:
          pattern: ^VirtualizedTableProps$
          location: TYPE_REFERENCE
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^VirtualizedTableProps$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
- ruleID: semver-packages-lib-utils-src-types-components-table-virtualizedtablebody-d-ts-rowmemo-removed-group-2
  labels:
  - change-type=removed
  - has-codemod=false
  - kind=constant
  - package=@openshift/dynamic-plugin-sdk-utils
  - source=semver-analyzer
  effort: 5
  category: mandatory
  description: 2 related changes
  message: |
    This rule contains 2 migration steps. Apply each one independently:

    Step 1 of 2:
    Constant 'RowMemo' was removed (constant): Exported constant `RowMemo` was removed

    File: packages/lib-utils/src/types/components/table/VirtualizedTableBody.d.ts
    Before: constant: RowMemo: MemoExoticComponent<(<destructured>: RowMemoProps) => JSX.Element>

    Step 2 of 2:
    Constant 'TableRow' was removed (constant): Exported constant `TableRow` was removed

    File: packages/lib-utils/src/types/components/table/VirtualizedTableBody.d.ts
    Before: constant: TableRow: FC<TableRowProps>
  when:
    or:
    - or:
      - frontend.referenced:
          pattern: ^RowMemo$
          location: JSX_COMPONENT
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^RowMemo$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
    - or:
      - frontend.referenced:
          pattern: ^TableRow$
          location: JSX_COMPONENT
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^TableRow$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
- ruleID: semver-packages-lib-utils-src-types-components-table-virtualizedtablebody-d-ts-rowmemoprops-removed-group-4
  labels:
  - change-type=removed
  - has-codemod=false
  - kind=type-alias
  - package=@openshift/dynamic-plugin-sdk-utils
  - source=semver-analyzer
  effort: 5
  category: mandatory
  description: 4 related changes
  message: |
    This rule contains 4 migration steps. Apply each one independently:

    Step 1 of 4:
    Type-alias 'RowMemoProps' was removed (type-alias): Exported type alias `RowMemoProps` was removed

    File: packages/lib-utils/src/types/components/table/VirtualizedTableBody.d.ts
    Before: type alias: RowMemoProps: RowProps<D> & {
        Row: React.ComponentType<RowProps<D>>;
    }

    Step 2 of 4:
    Type-alias 'RowProps' was removed (type-alias): Exported type alias `RowProps` was removed

    File: packages/lib-utils/src/types/components/table/VirtualizedTableBody.d.ts
    Before: type alias: RowProps: {
        /** Row data object. */
        obj: D;
        /** Row index */
        index: number;
    }

    Step 3 of 4:
    Type-alias 'TableColumn' was removed (type-alias): Exported type alias `TableColumn` was removed

    File: packages/lib-utils/src/types/components/table/VirtualizedTableBody.d.ts
    Before: type alias: TableColumn: ICell & {
        /** Column ID. */
        id: string;
        /** Optional sort configuration. */
        sort?: ((data: D[], sortDirection: SortByDirection) => D[]) | ThProps['sort'] | string;
        /** Optional visibility. */
        visibility?: string[];
    }

    Step 4 of 4:
    Type-alias 'TableRowProps' was removed (type-alias): Exported type alias `TableRowProps` was removed

    File: packages/lib-utils/src/types/components/table/VirtualizedTableBody.d.ts
    Before: type alias: TableRowProps: {
        /** Row ID. */
        id: string;
        /** Row index. */
        index: number;
        /** Optional row style. */
        style: object;
        /** Row key. */
        trKey: string;
        /** Optional className. */
        className?: string;
    }
  when:
    or:
    - or:
      - frontend.referenced:
          pattern: ^RowMemoProps$
          location: TYPE_REFERENCE
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^RowMemoProps$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
    - or:
      - frontend.referenced:
          pattern: ^RowProps$
          location: TYPE_REFERENCE
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^RowProps$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
    - or:
      - frontend.referenced:
          pattern: ^TableColumn$
          location: TYPE_REFERENCE
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^TableColumn$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
    - or:
      - frontend.referenced:
          pattern: ^TableRowProps$
          location: TYPE_REFERENCE
          from: '@openshift/dynamic-plugin-sdk-utils'
      - frontend.referenced:
          pattern: ^TableRowProps$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-utils'
- ruleID: semver-packages-lib-utils-src-types-k8s-hooks-usek8swatchresource-d-ts-usek8swatchresource-type-changed
  labels:
  - source=semver-analyzer
  - change-type=type-changed
  - kind=constant
  - has-codemod=false
  - package=@openshift/dynamic-plugin-sdk-utils
  effort: 3
  category: mandatory
  description: 'Return type of `useK8sWatchResource` changed from `<R extends K8sResourceCommon | K8sResourceCommon[]>(initResource: WatchK8sResource | null, options?: Partial<RequestInit & WebSocketOptions & { wsPrefix?: string | undefined; pathPrefix?: string | undefined }> | undefined) => WatchK8sResult<R>` to `<R extends K8sResourceCommon | K8sResourceCommon[]>(initResource: WatchK8sResource | null, initModel?: K8sModelCommon | undefined, options?: Partial<RequestInit & WebSocketOptions & { wsPrefix?: string | undefined; pathPrefix?: string | undefined }> | undefined) => WatchK8sResult<R>`'
  message: |-
    Constant 'useK8sWatchResource' had its type changed (constant): Return type of `useK8sWatchResource` changed from `<R extends K8sResourceCommon | K8sResourceCommon[]>(initResource: WatchK8sResource | null, options?: Partial<RequestInit & WebSocketOptions & { wsPrefix?: string | undefined; pathPrefix?: string | undefined }> | undefined) => WatchK8sResult<R>` to `<R extends K8sResourceCommon | K8sResourceCommon[]>(initResource: WatchK8sResource | null, initModel?: K8sModelCommon | undefined, options?: Partial<RequestInit & WebSocketOptions & { wsPrefix?: string | undefined; pathPrefix?: string | undefined }> | undefined) => WatchK8sResult<R>`

    File: packages/lib-utils/src/types/k8s/hooks/useK8sWatchResource.d.ts
    Before: <R extends K8sResourceCommon | K8sResourceCommon[]>(initResource: WatchK8sResource | null, options?: Partial<RequestInit & WebSocketOptions & { wsPrefix?: string | undefined; pathPrefix?: string | undefined }> | undefined) => WatchK8sResult<R>
    After: <R extends K8sResourceCommon | K8sResourceCommon[]>(initResource: WatchK8sResource | null, initModel?: K8sModelCommon | undefined, options?: Partial<RequestInit & WebSocketOptions & { wsPrefix?: string | undefined; pathPrefix?: string | undefined }> | undefined) => WatchK8sResult<R>
  when:
    frontend.referenced:
      pattern: ^useK8sWatchResource$
      location: IMPORT
      from: '@openshift/dynamic-plugin-sdk-utils'
- ruleID: semver-packages-lib-utils-src-types-k8s-hooks-usek8swatchresources-d-ts-usek8swatchresources-type-changed
  labels:
  - source=semver-analyzer
  - change-type=type-changed
  - kind=constant
  - has-codemod=false
  - package=@openshift/dynamic-plugin-sdk-utils
  effort: 3
  category: mandatory
  description: 'Return type of `useK8sWatchResources` changed from `<R extends ResourcesObject>(initResources: WatchK8sResources<R>, options?: Partial<RequestInit & WebSocketOptions & { wsPrefix?: string | undefined; pathPrefix?: string | undefined }> | undefined) => WatchK8sResults<R>` to `<R extends ResourcesObject>(initResources: WatchK8sResources<R>, initModels?: K8sModelCommon[] | undefined, options?: Partial<RequestInit & WebSocketOptions & { wsPrefix?: string | undefined; pathPrefix?: string | undefined }> | undefined) => WatchK8sResults<R>`'
  message: |-
    Constant 'useK8sWatchResources' had its type changed (constant): Return type of `useK8sWatchResources` changed from `<R extends ResourcesObject>(initResources: WatchK8sResources<R>, options?: Partial<RequestInit & WebSocketOptions & { wsPrefix?: string | undefined; pathPrefix?: string | undefined }> | undefined) => WatchK8sResults<R>` to `<R extends ResourcesObject>(initResources: WatchK8sResources<R>, initModels?: K8sModelCommon[] | undefined, options?: Partial<RequestInit & WebSocketOptions & { wsPrefix?: string | undefined; pathPrefix?: string | undefined }> | undefined) => WatchK8sResults<R>`

    File: packages/lib-utils/src/types/k8s/hooks/useK8sWatchResources.d.ts
    Before: <R extends ResourcesObject>(initResources: WatchK8sResources<R>, options?: Partial<RequestInit & WebSocketOptions & { wsPrefix?: string | undefined; pathPrefix?: string | undefined }> | undefined) => WatchK8sResults<R>
    After: <R extends ResourcesObject>(initResources: WatchK8sResources<R>, initModels?: K8sModelCommon[] | undefined, options?: Partial<RequestInit & WebSocketOptions & { wsPrefix?: string | undefined; pathPrefix?: string | undefined }> | undefined) => WatchK8sResults<R>
  when:
    frontend.referenced:
      pattern: ^useK8sWatchResources$
      location: IMPORT
      from: '@openshift/dynamic-plugin-sdk-utils'
- ruleID: semver-packages-lib-utils-src-types-k8s-k8s-utils-d-ts-getk8sresourceurl-type-changed
  labels:
  - source=semver-analyzer
  - change-type=type-changed
  - kind=constant
  - has-codemod=true
  - package=@openshift/dynamic-plugin-sdk-utils
  effort: 3
  category: mandatory
  description: 'Return type of `getK8sResourceURL` changed from `(model: K8sModelCommon, resource?: K8sResourceCommon | undefined, queryOptions?: QueryOptions) => string` to `(model: K8sModelCommon, resource?: K8sResourceCommon | undefined, queryOptions?: QueryOptions, isCreate?: boolean) => string`'
  message: |-
    Constant 'getK8sResourceURL' had its type changed (constant): Return type of `getK8sResourceURL` changed from `(model: K8sModelCommon, resource?: K8sResourceCommon | undefined, queryOptions?: QueryOptions) => string` to `(model: K8sModelCommon, resource?: K8sResourceCommon | undefined, queryOptions?: QueryOptions, isCreate?: boolean) => string`

    File: packages/lib-utils/src/types/k8s/k8s-utils.d.ts
    Before: (model: K8sModelCommon, resource?: K8sResourceCommon | undefined, queryOptions?: QueryOptions) => string
    After: (model: K8sModelCommon, resource?: K8sResourceCommon | undefined, queryOptions?: QueryOptions, isCreate?: boolean) => string
  when:
    frontend.referenced:
      pattern: ^getK8sResourceURL$
      location: IMPORT
      from: '@openshift/dynamic-plugin-sdk-utils'
- ruleID: semver-packages-lib-utils-src-types-types-k8s-d-ts-queryparams-type-changed
  labels:
  - source=semver-analyzer
  - change-type=type-changed
  - kind=type-alias
  - has-codemod=true
  - package=@openshift/dynamic-plugin-sdk-utils
  effort: 3
  category: mandatory
  description: |-
    Return type of `QueryParams` changed from `Partial<{
        watch: string;
        labelSelector: string;
        fieldSelector: string;
        resourceVersion: string;
        [key: string]: string;
    }>` to `Partial<{
        watch: string;
        labelSelector: string;
        fieldSelector: string;
        resourceVersion: string;
        pretty: string;
        dryRun: string;
        fieldManager: string;
        fieldValidation: string;
        [key: string]: string;
    }>`
  message: |-
    Type-alias 'QueryParams' had its type changed (type-alias): Return type of `QueryParams` changed from `Partial<{
        watch: string;
        labelSelector: string;
        fieldSelector: string;
        resourceVersion: string;
        [key: string]: string;
    }>` to `Partial<{
        watch: string;
        labelSelector: string;
        fieldSelector: string;
        resourceVersion: string;
        pretty: string;
        dryRun: string;
        fieldManager: string;
        fieldValidation: string;
        [key: string]: string;
    }>`

    File: packages/lib-utils/src/types/types/k8s.d.ts
    Before: Partial<{
        watch: string;
        labelSelector: string;
        fieldSelector: string;
        resourceVersion: string;
        [key: string]: string;
    }>
    After: Partial<{
        watch: string;
        labelSelector: string;
        fieldSelector: string;
        resourceVersion: string;
        pretty: string;
        dryRun: string;
        fieldManager: string;
        fieldValidation: string;
        [key: string]: string;
    }>
  when:
    or:
    - frontend.referenced:
        pattern: ^QueryParams$
        location: TYPE_REFERENCE
        from: '@openshift/dynamic-plugin-sdk-utils'
    - frontend.referenced:
        pattern: ^QueryParams$
        location: IMPORT
        from: '@openshift/dynamic-plugin-sdk-utils'
- ruleID: semver-packages-lib-webpack-src-types-common-src-errors-errorwithcause-errorwithcause-d-ts-errorwithcause-constructor-type-changed
  labels:
  - source=semver-analyzer
  - change-type=type-changed
  - kind=method
  - has-codemod=true
  - package=@openshift/dynamic-plugin-sdk-webpack
  effort: 3
  category: mandatory
  description: Parameter `cause` of `constructor` was made required
  message: |-
    Method 'ErrorWithCause.constructor' had its type changed (method): Parameter `cause` of `constructor` was made required

    File: packages/lib-webpack/src/types/common/src/errors/ErrorWithCause.ErrorWithCause.d.ts
    Before: optional
    After: required
  when:
    frontend.referenced:
      pattern: ^constructor$
      location: FUNCTION_CALL
      from: '@openshift/dynamic-plugin-sdk-webpack'
- ruleID: semver-packages-lib-webpack-src-types-lib-core-src-runtime-pluginloader-pluginloader-d-ts-pluginloader-processpluginmanifest-renamed
  labels:
  - source=semver-analyzer
  - change-type=renamed
  - kind=method
  - has-codemod=true
  - package=@openshift/dynamic-plugin-sdk-webpack
  effort: 1
  category: mandatory
  description: method `processPluginManifest` was renamed to `transformPluginManifest` in `PluginLoader`
  message: |-
    Method 'PluginLoader.processPluginManifest' was renamed (method): method `processPluginManifest` was renamed to `transformPluginManifest` in `PluginLoader`

    File: packages/lib-webpack/src/types/lib-core/src/runtime/PluginLoader.PluginLoader.d.ts
    Before: processPluginManifest
    After: transformPluginManifest
  when:
    frontend.referenced:
      pattern: ^processPluginManifest$
      location: FUNCTION_CALL
      from: '@openshift/dynamic-plugin-sdk-webpack'
- ruleID: semver-packages-lib-webpack-src-types-lib-core-src-runtime-pluginloader-pluginloader-d-ts-pluginloader-loadpluginmanifest-type-changed
  labels:
  - source=semver-analyzer
  - change-type=type-changed
  - kind=method
  - has-codemod=true
  - package=@openshift/dynamic-plugin-sdk-webpack
  effort: 3
  category: mandatory
  description: '`loadPluginManifest` was made sync (async wrapper removed)'
  message: |-
    Method 'PluginLoader.loadPluginManifest' had its type changed (method): `loadPluginManifest` was made sync (async wrapper removed)

    File: packages/lib-webpack/src/types/lib-core/src/runtime/PluginLoader.PluginLoader.d.ts
    Before: Promise<PluginManifest>
    After: Promise
  when:
    frontend.referenced:
      pattern: ^loadPluginManifest$
      location: FUNCTION_CALL
      from: '@openshift/dynamic-plugin-sdk-webpack'
- ruleID: semver-packages-lib-webpack-src-types-lib-core-src-runtime-pluginloader-d-ts-pluginloaderoptions-type-changed
  labels:
  - source=semver-analyzer
  - change-type=type-changed
  - kind=type-alias
  - has-codemod=true
  - package=@openshift/dynamic-plugin-sdk-webpack
  - change-scope=additive
  effort: 3
  category: mandatory
  description: |-
    Return type of `PluginLoaderOptions` changed from `Partial<{
        /**
         * Control which plugins can be loaded.
         *
         * The `reload` argument indicates whether an already loaded plugin is to be reloaded.
         *
         * By default, all plugins are allowed to be loaded.
         */
        canLoadPlugin: (manifest: PluginManifest, reload: boolean) => boolean;
        /**
         * Control whether the given plugin script can be reloaded when attempting to reload
         * the associated plugin.
         *
         * By default, all plugin scripts are allowed to be reloaded.
         */
        canReloadScript: (manifest: PluginManifest, scriptName: string) => boolean;
        /**
         * Customize the global callback function used by plugin entry scripts.
         *
         * This option applies only to plugins using the `callback` registration method.
         */
        entryCallbackSettings: Partial<{
            /**
             * Control whether to register the callback function.
             *
             * Default value: `true`.
             */
            registerCallback: boolean;
            /**
             * Name of the callback function.
             *
             * Default value: `__load_plugin_entry__`.
             */
            name: string;
        }>;
        /**
         * Custom resource fetch implementation.
         *
         * The custom implementation may specify any host application or environment specific
         * request headers that are necessary to fetch plugin resources over the network.
         *
         * By default, a basic {@link fetch} API based implementation is used.
         */
        fetchImpl: ResourceFetch;
        /**
         * Fixed resolutions used when processing plugin dependencies.
         *
         * There are two kinds of dependencies:
         * - Regular dependencies, which refer to actual plugins that must be loaded with
         *   the right version prior to loading the plugin that depends on them.
         * - Environment dependencies, which refer to any environment specific constants
         *   such as the host application's version, runtime platform version, etc.
         *
         * This option should be used to satisfy any environment specific dependencies
         * supported by the host application.
         *
         * Default value: empty object.
         */
        fixedPluginDependencyResolutions: Record<string, string>;
        /**
         * webpack share scope object for initializing `PluginEntryModule` containers.
         *
         * Host applications built with webpack should use dedicated webpack specific APIs
         * such as `__webpack_init_sharing__` and `__webpack_share_scopes__` to initialize
         * and access this object.
         *
         * Default value: empty object.
         *
         * @see https://webpack.js.org/concepts/module-federation/#dynamic-remote-containers
         */
        sharedScope: AnyObject;
        /**
         * Post-process the plugin manifest.
         *
         * By default, no post-processing is performed on the manifest.
         */
        postProcessManifest: (manifest: PluginManifest) => PluginManifest;
        /**
         * Provide access to the plugin's entry module.
         *
         * This option applies only to plugins using the `custom` registration method.
         *
         * For example, if a plugin was built with `var` library type (i.e. its entry module
         * is assigned to a variable), you can access the entry module via `window[pluginName]`.
         *
         * By default, this function does nothing.
         */
        getPluginEntryModule: (manifest: PluginManifest) => PluginEntryModule | void;
    }>` to `Partial<{
        /**
         * Control which plugins can be loaded.
         *
         * The `reload` argument indicates whether an already loaded plugin is to be reloaded.
         *
         * By default, all plugins are allowed to be loaded.
         */
        canLoadPlugin: (manifest: PluginManifest, reload: boolean) => boolean;
        /**
         * Control whether the given plugin script can be reloaded when attempting to reload
         * the associated plugin.
         *
         * By default, all plugin scripts are allowed to be reloaded.
         */
        canReloadScript: (manifest: PluginManifest, scriptName: string) => boolean;
        /**
         * Customize the global callback function used by plugin entry scripts.
         *
         * This option applies only to plugins using the `callback` registration method.
         */
        entryCallbackSettings: Partial<{
            /**
             * Control whether to register the callback function.
             *
             * Default value: `true`.
             */
            registerCallback: boolean;
            /**
             * Name of the callback function.
             *
             * Default value: `__load_plugin_entry__`.
             */
            name: string;
        }>;
        /**
         * Custom resource fetch implementation.
         *
         * The custom implementation may specify any host application or environment specific
         * request headers that are necessary to fetch plugin resources over the network.
         *
         * By default, a basic {@link fetch} API based implementation is used.
         */
        fetchImpl: ResourceFetch;
        /**
         * Fixed resolutions used when processing plugin dependencies.
         *
         * There are two kinds of dependencies:
         * - Regular dependencies, which refer to actual plugins that must be loaded with
         *   the right version prior to loading the plugin that depends on them.
         * - Environment dependencies, which refer to any environment specific constants
         *   such as the host application's version, runtime platform version, etc.
         *
         * This option should be used to satisfy any environment specific dependencies
         * supported by the host application.
         *
         * Default value: empty object.
         */
        fixedPluginDependencyResolutions: Record<string, string>;
        /**
         * webpack share scope object for initializing `PluginEntryModule` containers.
         *
         * Host applications built with webpack should use dedicated webpack specific APIs
         * such as `__webpack_init_sharing__` and `__webpack_share_scopes__` to initialize
         * and access this object.
         *
         * Default value: empty object.
         *
         * @see https://webpack.js.org/concepts/module-federation/#dynamic-remote-containers
         */
        sharedScope: AnyObject;
        /**
         * Transform the plugin manifest before loading the associated plugin.
         *
         * By default, no transformation is performed on the manifest.
         */
        transformPluginManifest: (manifest: PluginManifest) => PluginManifest;
        /**
         * Provide access to the plugin's entry module.
         *
         * This option applies only to plugins using the `custom` registration method.
         *
         * For example, if a plugin was built with `var` library type (i.e. its entry module
         * is assigned to a variable), you can access the entry module via `window[pluginName]`.
         *
         * By default, this function does nothing.
         */
        getPluginEntryModule: (manifest: PluginManifest) => PluginEntryModule | void;
    }>`
  message: |-
    Type-alias 'PluginLoaderOptions' had its type changed (type-alias): Return type of `PluginLoaderOptions` changed from `Partial<{
        /**
         * Control which plugins can be loaded.
         *
         * The `reload` argument indicates whether an already loaded plugin is to be reloaded.
         *
         * By default, all plugins are allowed to be loaded.
         */
        canLoadPlugin: (manifest: PluginManifest, reload: boolean) => boolean;
        /**
         * Control whether the given plugin script can be reloaded when attempting to reload
         * the associated plugin.
         *
         * By default, all plugin scripts are allowed to be reloaded.
         */
        canReloadScript: (manifest: PluginManifest, scriptName: string) => boolean;
        /**
         * Customize the global callback function used by plugin entry scripts.
         *
         * This option applies only to plugins using the `callback` registration method.
         */
        entryCallbackSettings: Partial<{
            /**
             * Control whether to register the callback function.
             *
             * Default value: `true`.
             */
            registerCallback: boolean;
            /**
             * Name of the callback function.
             *
             * Default value: `__load_plugin_entry__`.
             */
            name: string;
        }>;
        /**
         * Custom resource fetch implementation.
         *
         * The custom implementation may specify any host application or environment specific
         * request headers that are necessary to fetch plugin resources over the network.
         *
         * By default, a basic {@link fetch} API based implementation is used.
         */
        fetchImpl: ResourceFetch;
        /**
         * Fixed resolutions used when processing plugin dependencies.
         *
         * There are two kinds of dependencies:
         * - Regular dependencies, which refer to actual plugins that must be loaded with
         *   the right version prior to loading the plugin that depends on them.
         * - Environment dependencies, which refer to any environment specific constants
         *   such as the host application's version, runtime platform version, etc.
         *
         * This option should be used to satisfy any environment specific dependencies
         * supported by the host application.
         *
         * Default value: empty object.
         */
        fixedPluginDependencyResolutions: Record<string, string>;
        /**
         * webpack share scope object for initializing `PluginEntryModule` containers.
         *
         * Host applications built with webpack should use dedicated webpack specific APIs
         * such as `__webpack_init_sharing__` and `__webpack_share_scopes__` to initialize
         * and access this object.
         *
         * Default value: empty object.
         *
         * @see https://webpack.js.org/concepts/module-federation/#dynamic-remote-containers
         */
        sharedScope: AnyObject;
        /**
         * Post-process the plugin manifest.
         *
         * By default, no post-processing is performed on the manifest.
         */
        postProcessManifest: (manifest: PluginManifest) => PluginManifest;
        /**
         * Provide access to the plugin's entry module.
         *
         * This option applies only to plugins using the `custom` registration method.
         *
         * For example, if a plugin was built with `var` library type (i.e. its entry module
         * is assigned to a variable), you can access the entry module via `window[pluginName]`.
         *
         * By default, this function does nothing.
         */
        getPluginEntryModule: (manifest: PluginManifest) => PluginEntryModule | void;
    }>` to `Partial<{
        /**
         * Control which plugins can be loaded.
         *
         * The `reload` argument indicates whether an already loaded plugin is to be reloaded.
         *
         * By default, all plugins are allowed to be loaded.
         */
        canLoadPlugin: (manifest: PluginManifest, reload: boolean) => boolean;
        /**
         * Control whether the given plugin script can be reloaded when attempting to reload
         * the associated plugin.
         *
         * By default, all plugin scripts are allowed to be reloaded.
         */
        canReloadScript: (manifest: PluginManifest, scriptName: string) => boolean;
        /**
         * Customize the global callback function used by plugin entry scripts.
         *
         * This option applies only to plugins using the `callback` registration method.
         */
        entryCallbackSettings: Partial<{
            /**
             * Control whether to register the callback function.
             *
             * Default value: `true`.
             */
            registerCallback: boolean;
            /**
             * Name of the callback function.
             *
             * Default value: `__load_plugin_entry__`.
             */
            name: string;
        }>;
        /**
         * Custom resource fetch implementation.
         *
         * The custom implementation may specify any host application or environment specific
         * request headers that are necessary to fetch plugin resources over the network.
         *
         * By default, a basic {@link fetch} API based implementation is used.
         */
        fetchImpl: ResourceFetch;
        /**
         * Fixed resolutions used when processing plugin dependencies.
         *
         * There are two kinds of dependencies:
         * - Regular dependencies, which refer to actual plugins that must be loaded with
         *   the right version prior to loading the plugin that depends on them.
         * - Environment dependencies, which refer to any environment specific constants
         *   such as the host application's version, runtime platform version, etc.
         *
         * This option should be used to satisfy any environment specific dependencies
         * supported by the host application.
         *
         * Default value: empty object.
         */
        fixedPluginDependencyResolutions: Record<string, string>;
        /**
         * webpack share scope object for initializing `PluginEntryModule` containers.
         *
         * Host applications built with webpack should use dedicated webpack specific APIs
         * such as `__webpack_init_sharing__` and `__webpack_share_scopes__` to initialize
         * and access this object.
         *
         * Default value: empty object.
         *
         * @see https://webpack.js.org/concepts/module-federation/#dynamic-remote-containers
         */
        sharedScope: AnyObject;
        /**
         * Transform the plugin manifest before loading the associated plugin.
         *
         * By default, no transformation is performed on the manifest.
         */
        transformPluginManifest: (manifest: PluginManifest) => PluginManifest;
        /**
         * Provide access to the plugin's entry module.
         *
         * This option applies only to plugins using the `custom` registration method.
         *
         * For example, if a plugin was built with `var` library type (i.e. its entry module
         * is assigned to a variable), you can access the entry module via `window[pluginName]`.
         *
         * By default, this function does nothing.
         */
        getPluginEntryModule: (manifest: PluginManifest) => PluginEntryModule | void;
    }>`

    File: packages/lib-webpack/src/types/lib-core/src/runtime/PluginLoader.d.ts
    Before: Partial<{
        /**
         * Control which plugins can be loaded.
         *
         * The `reload` argument indicates whether an already loaded plugin is to be reloaded.
         *
         * By default, all plugins are allowed to be loaded.
         */
        canLoadPlugin: (manifest: PluginManifest, reload: boolean) => boolean;
        /**
         * Control whether the given plugin script can be reloaded when attempting to reload
         * the associated plugin.
         *
         * By default, all plugin scripts are allowed to be reloaded.
         */
        canReloadScript: (manifest: PluginManifest, scriptName: string) => boolean;
        /**
         * Customize the global callback function used by plugin entry scripts.
         *
         * This option applies only to plugins using the `callback` registration method.
         */
        entryCallbackSettings: Partial<{
            /**
             * Control whether to register the callback function.
             *
             * Default value: `true`.
             */
            registerCallback: boolean;
            /**
             * Name of the callback function.
             *
             * Default value: `__load_plugin_entry__`.
             */
            name: string;
        }>;
        /**
         * Custom resource fetch implementation.
         *
         * The custom implementation may specify any host application or environment specific
         * request headers that are necessary to fetch plugin resources over the network.
         *
         * By default, a basic {@link fetch} API based implementation is used.
         */
        fetchImpl: ResourceFetch;
        /**
         * Fixed resolutions used when processing plugin dependencies.
         *
         * There are two kinds of dependencies:
         * - Regular dependencies, which refer to actual plugins that must be loaded with
         *   the right version prior to loading the plugin that depends on them.
         * - Environment dependencies, which refer to any environment specific constants
         *   such as the host application's version, runtime platform version, etc.
         *
         * This option should be used to satisfy any environment specific dependencies
         * supported by the host application.
         *
         * Default value: empty object.
         */
        fixedPluginDependencyResolutions: Record<string, string>;
        /**
         * webpack share scope object for initializing `PluginEntryModule` containers.
         *
         * Host applications built with webpack should use dedicated webpack specific APIs
         * such as `__webpack_init_sharing__` and `__webpack_share_scopes__` to initialize
         * and access this object.
         *
         * Default value: empty object.
         *
         * @see https://webpack.js.org/concepts/module-federation/#dynamic-remote-containers
         */
        sharedScope: AnyObject;
        /**
         * Post-process the plugin manifest.
         *
         * By default, no post-processing is performed on the manifest.
         */
        postProcessManifest: (manifest: PluginManifest) => PluginManifest;
        /**
         * Provide access to the plugin's entry module.
         *
         * This option applies only to plugins using the `custom` registration method.
         *
         * For example, if a plugin was built with `var` library type (i.e. its entry module
         * is assigned to a variable), you can access the entry module via `window[pluginName]`.
         *
         * By default, this function does nothing.
         */
        getPluginEntryModule: (manifest: PluginManifest) => PluginEntryModule | void;
    }>
    After: Partial<{
        /**
         * Control which plugins can be loaded.
         *
         * The `reload` argument indicates whether an already loaded plugin is to be reloaded.
         *
         * By default, all plugins are allowed to be loaded.
         */
        canLoadPlugin: (manifest: PluginManifest, reload: boolean) => boolean;
        /**
         * Control whether the given plugin script can be reloaded when attempting to reload
         * the associated plugin.
         *
         * By default, all plugin scripts are allowed to be reloaded.
         */
        canReloadScript: (manifest: PluginManifest, scriptName: string) => boolean;
        /**
         * Customize the global callback function used by plugin entry scripts.
         *
         * This option applies only to plugins using the `callback` registration method.
         */
        entryCallbackSettings: Partial<{
            /**
             * Control whether to register the callback function.
             *
             * Default value: `true`.
             */
            registerCallback: boolean;
            /**
             * Name of the callback function.
             *
             * Default value: `__load_plugin_entry__`.
             */
            name: string;
        }>;
        /**
         * Custom resource fetch implementation.
         *
         * The custom implementation may specify any host application or environment specific
         * request headers that are necessary to fetch plugin resources over the network.
         *
         * By default, a basic {@link fetch} API based implementation is used.
         */
        fetchImpl: ResourceFetch;
        /**
         * Fixed resolutions used when processing plugin dependencies.
         *
         * There are two kinds of dependencies:
         * - Regular dependencies, which refer to actual plugins that must be loaded with
         *   the right version prior to loading the plugin that depends on them.
         * - Environment dependencies, which refer to any environment specific constants
         *   such as the host application's version, runtime platform version, etc.
         *
         * This option should be used to satisfy any environment specific dependencies
         * supported by the host application.
         *
         * Default value: empty object.
         */
        fixedPluginDependencyResolutions: Record<string, string>;
        /**
         * webpack share scope object for initializing `PluginEntryModule` containers.
         *
         * Host applications built with webpack should use dedicated webpack specific APIs
         * such as `__webpack_init_sharing__` and `__webpack_share_scopes__` to initialize
         * and access this object.
         *
         * Default value: empty object.
         *
         * @see https://webpack.js.org/concepts/module-federation/#dynamic-remote-containers
         */
        sharedScope: AnyObject;
        /**
         * Transform the plugin manifest before loading the associated plugin.
         *
         * By default, no transformation is performed on the manifest.
         */
        transformPluginManifest: (manifest: PluginManifest) => PluginManifest;
        /**
         * Provide access to the plugin's entry module.
         *
         * This option applies only to plugins using the `custom` registration method.
         *
         * For example, if a plugin was built with `var` library type (i.e. its entry module
         * is assigned to a variable), you can access the entry module via `window[pluginName]`.
         *
         * By default, this function does nothing.
         */
        getPluginEntryModule: (manifest: PluginManifest) => PluginEntryModule | void;
    }>
  when:
    or:
    - frontend.referenced:
        pattern: ^PluginLoaderOptions$
        location: TYPE_REFERENCE
        from: '@openshift/dynamic-plugin-sdk-webpack'
    - frontend.referenced:
        pattern: ^PluginLoaderOptions$
        location: IMPORT
        from: '@openshift/dynamic-plugin-sdk-webpack'
- ruleID: semver-packages-lib-webpack-src-types-lib-core-src-runtime-pluginstore-pluginstore-d-ts-pluginstore-constructor-type-changed
  labels:
  - source=semver-analyzer
  - change-type=type-changed
  - kind=method
  - has-codemod=true
  - package=@openshift/dynamic-plugin-sdk-webpack
  effort: 3
  category: mandatory
  description: Parameter `options` of `constructor` changed type from `PluginStoreOptions` to `PluginStoreLoaderSettings & PluginStoreOptions`
  message: |-
    Method 'PluginStore.constructor' had its type changed (method): Parameter `options` of `constructor` changed type from `PluginStoreOptions` to `PluginStoreLoaderSettings & PluginStoreOptions`

    File: packages/lib-webpack/src/types/lib-core/src/runtime/PluginStore.PluginStore.d.ts
    Before: PluginStoreOptions
    After: PluginStoreLoaderSettings & PluginStoreOptions
  when:
    frontend.referenced:
      pattern: ^constructor$
      location: FUNCTION_CALL
      from: '@openshift/dynamic-plugin-sdk-webpack'
- ruleID: semver-packages-lib-webpack-src-types-lib-core-src-runtime-pluginstore-d-ts-pluginstoreoptions-type-changed
  labels:
  - source=semver-analyzer
  - change-type=type-changed
  - kind=type-alias
  - has-codemod=false
  - package=@openshift/dynamic-plugin-sdk-webpack
  effort: 3
  category: mandatory
  description: |-
    Return type of `PluginStoreOptions` changed from `Partial<{
        /**
         * Options passed to `PluginLoader` instance managed by the `PluginStore`.
         *
         * Default value: empty object.
         */
        loaderOptions: PluginLoaderOptions;
        /**
         * Control whether to enable plugins automatically once they are loaded.
         *
         * Default value: `true`.
         */
        autoEnableLoadedPlugins: boolean;
    }>` to `Partial<{
        /**
         * Control whether to enable plugins automatically once they are loaded.
         *
         * Default value: `true`.
         */
        autoEnableLoadedPlugins: boolean;
    }>`
  message: |-
    Type-alias 'PluginStoreOptions' had its type changed (type-alias): Return type of `PluginStoreOptions` changed from `Partial<{
        /**
         * Options passed to `PluginLoader` instance managed by the `PluginStore`.
         *
         * Default value: empty object.
         */
        loaderOptions: PluginLoaderOptions;
        /**
         * Control whether to enable plugins automatically once they are loaded.
         *
         * Default value: `true`.
         */
        autoEnableLoadedPlugins: boolean;
    }>` to `Partial<{
        /**
         * Control whether to enable plugins automatically once they are loaded.
         *
         * Default value: `true`.
         */
        autoEnableLoadedPlugins: boolean;
    }>`

    File: packages/lib-webpack/src/types/lib-core/src/runtime/PluginStore.d.ts
    Before: Partial<{
        /**
         * Options passed to `PluginLoader` instance managed by the `PluginStore`.
         *
         * Default value: empty object.
         */
        loaderOptions: PluginLoaderOptions;
        /**
         * Control whether to enable plugins automatically once they are loaded.
         *
         * Default value: `true`.
         */
        autoEnableLoadedPlugins: boolean;
    }>
    After: Partial<{
        /**
         * Control whether to enable plugins automatically once they are loaded.
         *
         * Default value: `true`.
         */
        autoEnableLoadedPlugins: boolean;
    }>
  when:
    or:
    - frontend.referenced:
        pattern: ^PluginStoreOptions$
        location: TYPE_REFERENCE
        from: '@openshift/dynamic-plugin-sdk-webpack'
    - frontend.referenced:
        pattern: ^PluginStoreOptions$
        location: IMPORT
        from: '@openshift/dynamic-plugin-sdk-webpack'
- ruleID: semver-packages-lib-webpack-src-types-lib-core-src-runtime-pluginstorecontext-d-ts-pluginstoreproviderprops-type-changed
  labels:
  - source=semver-analyzer
  - change-type=type-changed
  - kind=type-alias
  - has-codemod=true
  - package=@openshift/dynamic-plugin-sdk-webpack
  effort: 3
  category: mandatory
  description: |-
    Return type of `PluginStoreProviderProps` changed from `React.PropsWithChildren<{
        store: PluginStore;
    }>` to `React.PropsWithChildren<{
        store: PluginStoreInterface;
    }>`
  message: |-
    Type-alias 'PluginStoreProviderProps' had its type changed (type-alias): Return type of `PluginStoreProviderProps` changed from `React.PropsWithChildren<{
        store: PluginStore;
    }>` to `React.PropsWithChildren<{
        store: PluginStoreInterface;
    }>`

    File: packages/lib-webpack/src/types/lib-core/src/runtime/PluginStoreContext.d.ts
    Before: React.PropsWithChildren<{
        store: PluginStore;
    }>
    After: React.PropsWithChildren<{
        store: PluginStoreInterface;
    }>
  when:
    or:
    - frontend.referenced:
        pattern: ^PluginStoreProviderProps$
        location: TYPE_REFERENCE
        from: '@openshift/dynamic-plugin-sdk-webpack'
    - frontend.referenced:
        pattern: ^PluginStoreProviderProps$
        location: IMPORT
        from: '@openshift/dynamic-plugin-sdk-webpack'
- ruleID: semver-packages-lib-webpack-src-types-lib-core-src-runtime-useresolvedextensions-d-ts-useresolvedextensions-type-changed
  labels:
  - source=semver-analyzer
  - change-type=type-changed
  - kind=constant
  - has-codemod=true
  - package=@openshift/dynamic-plugin-sdk-webpack
  effort: 3
  category: mandatory
  description: 'Return type of `useResolvedExtensions` changed from `<TExtension extends Extension<string, AnyObject>>(predicate?: ExtensionPredicate<TExtension> | undefined) => UseResolvedExtensionsResult<TExtension>` to `<TExtension extends Extension<string, AnyObject>>(predicate?: ExtensionPredicate<TExtension> | undefined, options?: UseResolvedExtensionsOptions) => UseResolvedExtensionsResult<TExtension>`'
  message: |-
    Constant 'useResolvedExtensions' had its type changed (constant): Return type of `useResolvedExtensions` changed from `<TExtension extends Extension<string, AnyObject>>(predicate?: ExtensionPredicate<TExtension> | undefined) => UseResolvedExtensionsResult<TExtension>` to `<TExtension extends Extension<string, AnyObject>>(predicate?: ExtensionPredicate<TExtension> | undefined, options?: UseResolvedExtensionsOptions) => UseResolvedExtensionsResult<TExtension>`

    File: packages/lib-webpack/src/types/lib-core/src/runtime/useResolvedExtensions.d.ts
    Before: <TExtension extends Extension<string, AnyObject>>(predicate?: ExtensionPredicate<TExtension> | undefined) => UseResolvedExtensionsResult<TExtension>
    After: <TExtension extends Extension<string, AnyObject>>(predicate?: ExtensionPredicate<TExtension> | undefined, options?: UseResolvedExtensionsOptions) => UseResolvedExtensionsResult<TExtension>
  when:
    frontend.referenced:
      pattern: ^useResolvedExtensions$
      location: IMPORT
      from: '@openshift/dynamic-plugin-sdk-webpack'
- ruleID: semver-packages-lib-webpack-src-types-lib-core-src-types-store-d-ts-pluginstoreinterface-type-changed
  labels:
  - source=semver-analyzer
  - change-type=type-changed
  - kind=type-alias
  - has-codemod=true
  - package=@openshift/dynamic-plugin-sdk-webpack
  - change-scope=additive
  effort: 3
  category: mandatory
  description: |-
    Return type of `PluginStoreInterface` changed from `{
        /**
         * Version of the `@openshift/dynamic-plugin-sdk` package.
         */
        readonly sdkVersion: string;
        /**
         * Subscribe to events emitted by the `PluginStore`.
         *
         * See {@link PluginEventType} for information on specific event types.
         *
         * Returns a function for unsubscribing the provided listener.
         */
        subscribe: (eventTypes: PluginEventType[], listener: VoidFunction) => VoidFunction;
        /**
         * Get extensions which are currently in use.
         *
         * An extension is in use when the associated plugin is currently enabled and its
         * feature flag requirements (if any) are met according to current feature flags.
         *
         * If you need to enhance or modify existing extension objects after the associated
         * plugin has been loaded and processed, we recommend using a custom React hook which
         * calls `useExtensions` or `useResolvedExtensions` and returns new extension object
         * instances.
         *
         * This method always returns a new array instance.
         */
        getExtensions: () => LoadedExtension[];
        /**
         * Get current information about plugins.
         *
         * This method always returns a new array instance.
         */
        getPluginInfo: () => PluginInfoEntry[];
        /**
         * Get current feature flags.
         *
         * This method always returns a new object.
         */
        getFeatureFlags: () => FeatureFlags;
        /**
         * Merge the entries of `newFlags` with current feature flags (non-boolean values
         * will be discarded).
         */
        setFeatureFlags: (newFlags: FeatureFlags) => void;
        /**
         * Start loading a plugin from the given manifest.
         *
         * The plugin manifest can be provided directly as an object or referenced via URL.
         *
         * Depending on the plugin's current load status, this method works as follows:
         * - plugin is loading - do nothing
         * - plugin has been loaded - reload only if `forceReload` is `true`
         * - plugin has failed to load - always reload
         *
         * The resulting Promise resolves when the load operation is complete. If the given
         * plugin is still loading when this method is invoked, the same Promise instance that
         * represents the load operation is returned.
         *
         * Use the `subscribe` method to respond to events emitted by the `PluginStore`.
         *
         * Be advised that any plugin modules which are already loaded by the host application
         * will _not_ be replaced upon reloading the associated plugin. This is due to webpack
         * module caching which also applies to federated modules. If a host application detects
         * changes in a plugin's deployment, users should be prompted to reload the application
         * to ensure all plugin modules in use are up to date.
         */
        loadPlugin: (manifest: PluginManifest | string, forceReload?: boolean) => Promise<void>;
        /**
         * Enable the given plugin(s).
         *
         * Enabling a plugin puts all of its extensions into use.
         */
        enablePlugins: (pluginNames: string[]) => void;
        /**
         * Disable the given plugin(s) with an optional reason.
         *
         * Disabling a plugin puts all of its extensions out of use.
         */
        disablePlugins: (pluginNames: string[], disableReason?: string) => void;
        /**
         * Get a specific module exposed by the given plugin.
         */
        getExposedModule: <TModule extends AnyObject>(pluginName: string, moduleName: string) => Promise<TModule>;
    }` to `{
        /**
         * Version of the `@openshift/dynamic-plugin-sdk` package.
         */
        readonly sdkVersion: string;
        /**
         * Subscribe to events emitted by the `PluginStore`.
         *
         * See {@link PluginEventType} for information on specific event types.
         *
         * Returns a function for unsubscribing the provided listener.
         */
        subscribe: (eventTypes: PluginEventType[], listener: VoidFunction) => VoidFunction;
        /**
         * Get extensions which are currently in use.
         *
         * An extension is in use when the associated plugin is currently enabled and its
         * feature flag requirements (if any) are met according to current feature flags.
         *
         * If you need to enhance or modify existing extension objects after the associated
         * plugin has been loaded and processed, we recommend using a custom React hook which
         * calls `useExtensions` or `useResolvedExtensions` and returns new extension object
         * instances.
         *
         * This method always returns a new array instance.
         */
        getExtensions: () => LoadedExtension[];
        /**
         * Get current information about plugins.
         *
         * This method always returns a new array instance.
         */
        getPluginInfo: () => PluginInfoEntry[];
        /**
         * Get current feature flags.
         *
         * This method always returns a new object.
         */
        getFeatureFlags: () => FeatureFlags;
        /**
         * Merge the entries of `newFlags` with current feature flags (non-boolean values
         * will be discarded).
         */
        setFeatureFlags: (newFlags: FeatureFlags) => void;
        /**
         * Start loading a plugin from the given manifest.
         *
         * The plugin manifest can be provided directly as an object or referenced via URL.
         *
         * Depending on the plugin's current load status, this method works as follows:
         * - plugin is loading - do nothing
         * - plugin has been loaded - reload only if `forceReload` is `true`
         * - plugin has failed to load - always reload
         *
         * The resulting Promise resolves when the load operation is complete. If the given
         * plugin is still loading when this method is invoked, the same Promise instance that
         * represents the load operation is returned.
         *
         * The resulting Promise rejects only when the plugin manifest cannot be loaded or
         * processed by the `PluginLoader` implementation.
         *
         * Use the `subscribe` method to respond to events emitted by the `PluginStore`.
         *
         * Be advised that any plugin modules which are already loaded by the host application
         * will _not_ be replaced upon reloading the associated plugin. This is due to webpack
         * module caching which also applies to federated modules. If a host application detects
         * changes in a plugin's deployment, users should be prompted to reload the application
         * to ensure all plugin modules in use are up to date.
         */
        loadPlugin: (manifest: PluginManifest | string, forceReload?: boolean) => Promise<void>;
        /**
         * Enable the given plugin(s).
         *
         * Enabling a plugin puts all of its extensions into use.
         */
        enablePlugins: (pluginNames: string[]) => void;
        /**
         * Disable the given plugin(s) with an optional reason.
         *
         * Disabling a plugin puts all of its extensions out of use.
         */
        disablePlugins: (pluginNames: string[], disableReason?: string) => void;
        /**
         * Get a specific module exposed by the given plugin.
         */
        getExposedModule: <TModule extends AnyObject>(pluginName: string, moduleName: string) => Promise<TModule>;
    }`
  message: |-
    Type-alias 'PluginStoreInterface' had its type changed (type-alias): Return type of `PluginStoreInterface` changed from `{
        /**
         * Version of the `@openshift/dynamic-plugin-sdk` package.
         */
        readonly sdkVersion: string;
        /**
         * Subscribe to events emitted by the `PluginStore`.
         *
         * See {@link PluginEventType} for information on specific event types.
         *
         * Returns a function for unsubscribing the provided listener.
         */
        subscribe: (eventTypes: PluginEventType[], listener: VoidFunction) => VoidFunction;
        /**
         * Get extensions which are currently in use.
         *
         * An extension is in use when the associated plugin is currently enabled and its
         * feature flag requirements (if any) are met according to current feature flags.
         *
         * If you need to enhance or modify existing extension objects after the associated
         * plugin has been loaded and processed, we recommend using a custom React hook which
         * calls `useExtensions` or `useResolvedExtensions` and returns new extension object
         * instances.
         *
         * This method always returns a new array instance.
         */
        getExtensions: () => LoadedExtension[];
        /**
         * Get current information about plugins.
         *
         * This method always returns a new array instance.
         */
        getPluginInfo: () => PluginInfoEntry[];
        /**
         * Get current feature flags.
         *
         * This method always returns a new object.
         */
        getFeatureFlags: () => FeatureFlags;
        /**
         * Merge the entries of `newFlags` with current feature flags (non-boolean values
         * will be discarded).
         */
        setFeatureFlags: (newFlags: FeatureFlags) => void;
        /**
         * Start loading a plugin from the given manifest.
         *
         * The plugin manifest can be provided directly as an object or referenced via URL.
         *
         * Depending on the plugin's current load status, this method works as follows:
         * - plugin is loading - do nothing
         * - plugin has been loaded - reload only if `forceReload` is `true`
         * - plugin has failed to load - always reload
         *
         * The resulting Promise resolves when the load operation is complete. If the given
         * plugin is still loading when this method is invoked, the same Promise instance that
         * represents the load operation is returned.
         *
         * Use the `subscribe` method to respond to events emitted by the `PluginStore`.
         *
         * Be advised that any plugin modules which are already loaded by the host application
         * will _not_ be replaced upon reloading the associated plugin. This is due to webpack
         * module caching which also applies to federated modules. If a host application detects
         * changes in a plugin's deployment, users should be prompted to reload the application
         * to ensure all plugin modules in use are up to date.
         */
        loadPlugin: (manifest: PluginManifest | string, forceReload?: boolean) => Promise<void>;
        /**
         * Enable the given plugin(s).
         *
         * Enabling a plugin puts all of its extensions into use.
         */
        enablePlugins: (pluginNames: string[]) => void;
        /**
         * Disable the given plugin(s) with an optional reason.
         *
         * Disabling a plugin puts all of its extensions out of use.
         */
        disablePlugins: (pluginNames: string[], disableReason?: string) => void;
        /**
         * Get a specific module exposed by the given plugin.
         */
        getExposedModule: <TModule extends AnyObject>(pluginName: string, moduleName: string) => Promise<TModule>;
    }` to `{
        /**
         * Version of the `@openshift/dynamic-plugin-sdk` package.
         */
        readonly sdkVersion: string;
        /**
         * Subscribe to events emitted by the `PluginStore`.
         *
         * See {@link PluginEventType} for information on specific event types.
         *
         * Returns a function for unsubscribing the provided listener.
         */
        subscribe: (eventTypes: PluginEventType[], listener: VoidFunction) => VoidFunction;
        /**
         * Get extensions which are currently in use.
         *
         * An extension is in use when the associated plugin is currently enabled and its
         * feature flag requirements (if any) are met according to current feature flags.
         *
         * If you need to enhance or modify existing extension objects after the associated
         * plugin has been loaded and processed, we recommend using a custom React hook which
         * calls `useExtensions` or `useResolvedExtensions` and returns new extension object
         * instances.
         *
         * This method always returns a new array instance.
         */
        getExtensions: () => LoadedExtension[];
        /**
         * Get current information about plugins.
         *
         * This method always returns a new array instance.
         */
        getPluginInfo: () => PluginInfoEntry[];
        /**
         * Get current feature flags.
         *
         * This method always returns a new object.
         */
        getFeatureFlags: () => FeatureFlags;
        /**
         * Merge the entries of `newFlags` with current feature flags (non-boolean values
         * will be discarded).
         */
        setFeatureFlags: (newFlags: FeatureFlags) => void;
        /**
         * Start loading a plugin from the given manifest.
         *
         * The plugin manifest can be provided directly as an object or referenced via URL.
         *
         * Depending on the plugin's current load status, this method works as follows:
         * - plugin is loading - do nothing
         * - plugin has been loaded - reload only if `forceReload` is `true`
         * - plugin has failed to load - always reload
         *
         * The resulting Promise resolves when the load operation is complete. If the given
         * plugin is still loading when this method is invoked, the same Promise instance that
         * represents the load operation is returned.
         *
         * The resulting Promise rejects only when the plugin manifest cannot be loaded or
         * processed by the `PluginLoader` implementation.
         *
         * Use the `subscribe` method to respond to events emitted by the `PluginStore`.
         *
         * Be advised that any plugin modules which are already loaded by the host application
         * will _not_ be replaced upon reloading the associated plugin. This is due to webpack
         * module caching which also applies to federated modules. If a host application detects
         * changes in a plugin's deployment, users should be prompted to reload the application
         * to ensure all plugin modules in use are up to date.
         */
        loadPlugin: (manifest: PluginManifest | string, forceReload?: boolean) => Promise<void>;
        /**
         * Enable the given plugin(s).
         *
         * Enabling a plugin puts all of its extensions into use.
         */
        enablePlugins: (pluginNames: string[]) => void;
        /**
         * Disable the given plugin(s) with an optional reason.
         *
         * Disabling a plugin puts all of its extensions out of use.
         */
        disablePlugins: (pluginNames: string[], disableReason?: string) => void;
        /**
         * Get a specific module exposed by the given plugin.
         */
        getExposedModule: <TModule extends AnyObject>(pluginName: string, moduleName: string) => Promise<TModule>;
    }`

    File: packages/lib-webpack/src/types/lib-core/src/types/store.d.ts
    Before: {
        /**
         * Version of the `@openshift/dynamic-plugin-sdk` package.
         */
        readonly sdkVersion: string;
        /**
         * Subscribe to events emitted by the `PluginStore`.
         *
         * See {@link PluginEventType} for information on specific event types.
         *
         * Returns a function for unsubscribing the provided listener.
         */
        subscribe: (eventTypes: PluginEventType[], listener: VoidFunction) => VoidFunction;
        /**
         * Get extensions which are currently in use.
         *
         * An extension is in use when the associated plugin is currently enabled and its
         * feature flag requirements (if any) are met according to current feature flags.
         *
         * If you need to enhance or modify existing extension objects after the associated
         * plugin has been loaded and processed, we recommend using a custom React hook which
         * calls `useExtensions` or `useResolvedExtensions` and returns new extension object
         * instances.
         *
         * This method always returns a new array instance.
         */
        getExtensions: () => LoadedExtension[];
        /**
         * Get current information about plugins.
         *
         * This method always returns a new array instance.
         */
        getPluginInfo: () => PluginInfoEntry[];
        /**
         * Get current feature flags.
         *
         * This method always returns a new object.
         */
        getFeatureFlags: () => FeatureFlags;
        /**
         * Merge the entries of `newFlags` with current feature flags (non-boolean values
         * will be discarded).
         */
        setFeatureFlags: (newFlags: FeatureFlags) => void;
        /**
         * Start loading a plugin from the given manifest.
         *
         * The plugin manifest can be provided directly as an object or referenced via URL.
         *
         * Depending on the plugin's current load status, this method works as follows:
         * - plugin is loading - do nothing
         * - plugin has been loaded - reload only if `forceReload` is `true`
         * - plugin has failed to load - always reload
         *
         * The resulting Promise resolves when the load operation is complete. If the given
         * plugin is still loading when this method is invoked, the same Promise instance that
         * represents the load operation is returned.
         *
         * Use the `subscribe` method to respond to events emitted by the `PluginStore`.
         *
         * Be advised that any plugin modules which are already loaded by the host application
         * will _not_ be replaced upon reloading the associated plugin. This is due to webpack
         * module caching which also applies to federated modules. If a host application detects
         * changes in a plugin's deployment, users should be prompted to reload the application
         * to ensure all plugin modules in use are up to date.
         */
        loadPlugin: (manifest: PluginManifest | string, forceReload?: boolean) => Promise<void>;
        /**
         * Enable the given plugin(s).
         *
         * Enabling a plugin puts all of its extensions into use.
         */
        enablePlugins: (pluginNames: string[]) => void;
        /**
         * Disable the given plugin(s) with an optional reason.
         *
         * Disabling a plugin puts all of its extensions out of use.
         */
        disablePlugins: (pluginNames: string[], disableReason?: string) => void;
        /**
         * Get a specific module exposed by the given plugin.
         */
        getExposedModule: <TModule extends AnyObject>(pluginName: string, moduleName: string) => Promise<TModule>;
    }
    After: {
        /**
         * Version of the `@openshift/dynamic-plugin-sdk` package.
         */
        readonly sdkVersion: string;
        /**
         * Subscribe to events emitted by the `PluginStore`.
         *
         * See {@link PluginEventType} for information on specific event types.
         *
         * Returns a function for unsubscribing the provided listener.
         */
        subscribe: (eventTypes: PluginEventType[], listener: VoidFunction) => VoidFunction;
        /**
         * Get extensions which are currently in use.
         *
         * An extension is in use when the associated plugin is currently enabled and its
         * feature flag requirements (if any) are met according to current feature flags.
         *
         * If you need to enhance or modify existing extension objects after the associated
         * plugin has been loaded and processed, we recommend using a custom React hook which
         * calls `useExtensions` or `useResolvedExtensions` and returns new extension object
         * instances.
         *
         * This method always returns a new array instance.
         */
        getExtensions: () => LoadedExtension[];
        /**
         * Get current information about plugins.
         *
         * This method always returns a new array instance.
         */
        getPluginInfo: () => PluginInfoEntry[];
        /**
         * Get current feature flags.
         *
         * This method always returns a new object.
         */
        getFeatureFlags: () => FeatureFlags;
        /**
         * Merge the entries of `newFlags` with current feature flags (non-boolean values
         * will be discarded).
         */
        setFeatureFlags: (newFlags: FeatureFlags) => void;
        /**
         * Start loading a plugin from the given manifest.
         *
         * The plugin manifest can be provided directly as an object or referenced via URL.
         *
         * Depending on the plugin's current load status, this method works as follows:
         * - plugin is loading - do nothing
         * - plugin has been loaded - reload only if `forceReload` is `true`
         * - plugin has failed to load - always reload
         *
         * The resulting Promise resolves when the load operation is complete. If the given
         * plugin is still loading when this method is invoked, the same Promise instance that
         * represents the load operation is returned.
         *
         * The resulting Promise rejects only when the plugin manifest cannot be loaded or
         * processed by the `PluginLoader` implementation.
         *
         * Use the `subscribe` method to respond to events emitted by the `PluginStore`.
         *
         * Be advised that any plugin modules which are already loaded by the host application
         * will _not_ be replaced upon reloading the associated plugin. This is due to webpack
         * module caching which also applies to federated modules. If a host application detects
         * changes in a plugin's deployment, users should be prompted to reload the application
         * to ensure all plugin modules in use are up to date.
         */
        loadPlugin: (manifest: PluginManifest | string, forceReload?: boolean) => Promise<void>;
        /**
         * Enable the given plugin(s).
         *
         * Enabling a plugin puts all of its extensions into use.
         */
        enablePlugins: (pluginNames: string[]) => void;
        /**
         * Disable the given plugin(s) with an optional reason.
         *
         * Disabling a plugin puts all of its extensions out of use.
         */
        disablePlugins: (pluginNames: string[], disableReason?: string) => void;
        /**
         * Get a specific module exposed by the given plugin.
         */
        getExposedModule: <TModule extends AnyObject>(pluginName: string, moduleName: string) => Promise<TModule>;
    }
  when:
    or:
    - frontend.referenced:
        pattern: ^PluginStoreInterface$
        location: TYPE_REFERENCE
        from: '@openshift/dynamic-plugin-sdk-webpack'
    - frontend.referenced:
        pattern: ^PluginStoreInterface$
        location: IMPORT
        from: '@openshift/dynamic-plugin-sdk-webpack'
- ruleID: semver-packages-lib-webpack-src-types-lib-webpack-src-webpack-dynamicremoteplugin-d-ts-dynamicremotepluginoptions-type-changed-group-2
  labels:
  - change-type=type-changed
  - has-codemod=true
  - kind=type-alias
  - package=@openshift/dynamic-plugin-sdk-webpack
  - source=semver-analyzer
  effort: 3
  category: mandatory
  description: 2 related changes
  message: |
    This rule contains 2 migration steps. Apply each one independently:

    Step 1 of 2:
    Type-alias 'DynamicRemotePluginOptions' had its type changed (type-alias): Return type of `DynamicRemotePluginOptions` changed from `{
        /**
         * Plugin build metadata.
         */
        pluginMetadata: PluginBuildMetadata;
        /**
         * List of extensions contributed by the plugin.
         */
        extensions: EncodedExtension[];
        /**
         * Modules shared between the host application and its plugins at runtime.
         *
         * It is the host application's responsibility to initialize and maintain its shared
         * scope object, and to communicate information about application provided modules to
         * its plugins.
         *
         * Default value: empty object.
         */
        sharedModules?: WebpackSharedObject;
        /**
         * Customize the webpack `ModuleFederationPlugin` options.
         */
        moduleFederationSettings?: PluginModuleFederationSettings;
        /**
         * Customize the call to global callback function in the plugin entry script.
         *
         * This option applies only if the module federation library type is `jsonp`.
         */
        entryCallbackSettings?: PluginEntryCallbackSettings;
        /**
         * Customize the filename of the generated plugin entry script.
         *
         * We recommend using the `[fullhash]` placeholder in production builds.
         *
         * Default value: `plugin-entry.js`.
         *
         * @see https://webpack.js.org/configuration/output/#outputfilename
         */
        entryScriptFilename?: string;
        /**
         * Customize the filename of the generated plugin manifest.
         *
         * Default value: `plugin-manifest.json`.
         */
        pluginManifestFilename?: string;
    }` to `{
        /**
         * Plugin build metadata.
         */
        pluginMetadata: PluginBuildMetadata;
        /**
         * List of extensions contributed by the plugin.
         */
        extensions: EncodedExtension[];
        /**
         * Modules shared between the host application and its plugins at runtime.
         *
         * It is the host application's responsibility to initialize and maintain its shared
         * scope object, and to communicate information about application provided modules to
         * its plugins.
         *
         * Default value: empty object.
         */
        sharedModules?: WebpackSharedObject;
        /**
         * Customize the webpack `ModuleFederationPlugin` options.
         */
        moduleFederationSettings?: PluginModuleFederationSettings;
        /**
         * Customize the call to global callback function in the plugin entry script.
         *
         * This option applies only if the module federation library type is `jsonp`.
         */
        entryCallbackSettings?: PluginEntryCallbackSettings;
        /**
         * Customize the filename of the generated plugin entry script.
         *
         * We recommend using `[contenthash]` or `[fullhash]` placeholder in production builds,
         * e.g. `plugin-entry.[contenthash].min.js`.
         *
         * Default value: `plugin-entry.js`.
         *
         * @see https://webpack.js.org/configuration/output/#outputfilename
         */
        entryScriptFilename?: string;
        /**
         * Customize the filename of the generated plugin manifest.
         *
         * Default value: `plugin-manifest.json`.
         */
        pluginManifestFilename?: string;
        /**
         * Transform the plugin manifest before emitting the asset to webpack compilation.
         *
         * Environment specific properties should be set via the `customProperties` object.
         *
         * By default, no transformation is performed on the manifest.
         */
        transformPluginManifest?: (manifest: PluginManifest) => PluginManifest;
    }`

    File: packages/lib-webpack/src/types/lib-webpack/src/webpack/DynamicRemotePlugin.d.ts
    Before: {
        /**
         * Plugin build metadata.
         */
        pluginMetadata: PluginBuildMetadata;
        /**
         * List of extensions contributed by the plugin.
         */
        extensions: EncodedExtension[];
        /**
         * Modules shared between the host application and its plugins at runtime.
         *
         * It is the host application's responsibility to initialize and maintain its shared
         * scope object, and to communicate information about application provided modules to
         * its plugins.
         *
         * Default value: empty object.
         */
        sharedModules?: WebpackSharedObject;
        /**
         * Customize the webpack `ModuleFederationPlugin` options.
         */
        moduleFederationSettings?: PluginModuleFederationSettings;
        /**
         * Customize the call to global callback function in the plugin entry script.
         *
         * This option applies only if the module federation library type is `jsonp`.
         */
        entryCallbackSettings?: PluginEntryCallbackSettings;
        /**
         * Customize the filename of the generated plugin entry script.
         *
         * We recommend using the `[fullhash]` placeholder in production builds.
         *
         * Default value: `plugin-entry.js`.
         *
         * @see https://webpack.js.org/configuration/output/#outputfilename
         */
        entryScriptFilename?: string;
        /**
         * Customize the filename of the generated plugin manifest.
         *
         * Default value: `plugin-manifest.json`.
         */
        pluginManifestFilename?: string;
    }
    After: {
        /**
         * Plugin build metadata.
         */
        pluginMetadata: PluginBuildMetadata;
        /**
         * List of extensions contributed by the plugin.
         */
        extensions: EncodedExtension[];
        /**
         * Modules shared between the host application and its plugins at runtime.
         *
         * It is the host application's responsibility to initialize and maintain its shared
         * scope object, and to communicate information about application provided modules to
         * its plugins.
         *
         * Default value: empty object.
         */
        sharedModules?: WebpackSharedObject;
        /**
         * Customize the webpack `ModuleFederationPlugin` options.
         */
        moduleFederationSettings?: PluginModuleFederationSettings;
        /**
         * Customize the call to global callback function in the plugin entry script.
         *
         * This option applies only if the module federation library type is `jsonp`.
         */
        entryCallbackSettings?: PluginEntryCallbackSettings;
        /**
         * Customize the filename of the generated plugin entry script.
         *
         * We recommend using `[contenthash]` or `[fullhash]` placeholder in production builds,
         * e.g. `plugin-entry.[contenthash].min.js`.
         *
         * Default value: `plugin-entry.js`.
         *
         * @see https://webpack.js.org/configuration/output/#outputfilename
         */
        entryScriptFilename?: string;
        /**
         * Customize the filename of the generated plugin manifest.
         *
         * Default value: `plugin-manifest.json`.
         */
        pluginManifestFilename?: string;
        /**
         * Transform the plugin manifest before emitting the asset to webpack compilation.
         *
         * Environment specific properties should be set via the `customProperties` object.
         *
         * By default, no transformation is performed on the manifest.
         */
        transformPluginManifest?: (manifest: PluginManifest) => PluginManifest;
    }

    Step 2 of 2:
    Type-alias 'PluginModuleFederationSettings' had its type changed (type-alias): Return type of `PluginModuleFederationSettings` changed from `Partial<{
        /**
         * Configure how the plugin entry module will be exposed at runtime.
         *
         * This value is passed to `ModuleFederationPlugin` as `library.type`.
         *
         * Note that `library.name` will be set to the name of the plugin.
         *
         * Default value: `jsonp`.
         *
         * @see https://webpack.js.org/configuration/output/#outputlibrarytype
         */
        libraryType: string;
        /**
         * The name of webpack share scope object used to share modules between the host
         * application and its plugins.
         *
         * This option applies only if the target host application is built with webpack
         * and uses dedicated webpack specific APIs such as `__webpack_init_sharing__`
         * and `__webpack_share_scopes__` to initialize and access this object.
         *
         * If the target host application is not built with webpack, plugins will not be
         * able to contribute new modules into the share scope object; however, plugins
         * will still be able to use any application provided shared modules.
         *
         * Default value: `default`.
         *
         * @see https://webpack.js.org/plugins/module-federation-plugin/#sharescope
         */
        sharedScope: string;
    }>` to `Partial<{
        /**
         * Configure how the plugin entry module will be exposed at runtime.
         *
         * This value is passed to `ModuleFederationPlugin` as `library.type`.
         *
         * Note that `library.name` will be set to the name of the plugin.
         *
         * Default value: `jsonp`.
         *
         * @see https://webpack.js.org/configuration/output/#outputlibrarytype
         */
        libraryType: string;
        /**
         * The name of webpack share scope object used to share modules between the host
         * application and its plugins.
         *
         * This option applies only if the target host application is built with webpack
         * and uses dedicated webpack specific APIs such as `__webpack_init_sharing__`
         * and `__webpack_share_scopes__` to initialize and access this object.
         *
         * If the target host application is not built with webpack, plugins will not be
         * able to contribute new modules into the share scope object; however, plugins
         * will still be able to use any application provided shared modules.
         *
         * Default value: `default`.
         *
         * @see https://webpack.js.org/plugins/module-federation-plugin/#sharescope
         */
        sharedScopeName: string;
    }>`

    File: packages/lib-webpack/src/types/lib-webpack/src/webpack/DynamicRemotePlugin.d.ts
    Before: Partial<{
        /**
         * Configure how the plugin entry module will be exposed at runtime.
         *
         * This value is passed to `ModuleFederationPlugin` as `library.type`.
         *
         * Note that `library.name` will be set to the name of the plugin.
         *
         * Default value: `jsonp`.
         *
         * @see https://webpack.js.org/configuration/output/#outputlibrarytype
         */
        libraryType: string;
        /**
         * The name of webpack share scope object used to share modules between the host
         * application and its plugins.
         *
         * This option applies only if the target host application is built with webpack
         * and uses dedicated webpack specific APIs such as `__webpack_init_sharing__`
         * and `__webpack_share_scopes__` to initialize and access this object.
         *
         * If the target host application is not built with webpack, plugins will not be
         * able to contribute new modules into the share scope object; however, plugins
         * will still be able to use any application provided shared modules.
         *
         * Default value: `default`.
         *
         * @see https://webpack.js.org/plugins/module-federation-plugin/#sharescope
         */
        sharedScope: string;
    }>
    After: Partial<{
        /**
         * Configure how the plugin entry module will be exposed at runtime.
         *
         * This value is passed to `ModuleFederationPlugin` as `library.type`.
         *
         * Note that `library.name` will be set to the name of the plugin.
         *
         * Default value: `jsonp`.
         *
         * @see https://webpack.js.org/configuration/output/#outputlibrarytype
         */
        libraryType: string;
        /**
         * The name of webpack share scope object used to share modules between the host
         * application and its plugins.
         *
         * This option applies only if the target host application is built with webpack
         * and uses dedicated webpack specific APIs such as `__webpack_init_sharing__`
         * and `__webpack_share_scopes__` to initialize and access this object.
         *
         * If the target host application is not built with webpack, plugins will not be
         * able to contribute new modules into the share scope object; however, plugins
         * will still be able to use any application provided shared modules.
         *
         * Default value: `default`.
         *
         * @see https://webpack.js.org/plugins/module-federation-plugin/#sharescope
         */
        sharedScopeName: string;
    }>
  when:
    or:
    - or:
      - frontend.referenced:
          pattern: ^DynamicRemotePluginOptions$
          location: TYPE_REFERENCE
          from: '@openshift/dynamic-plugin-sdk-webpack'
      - frontend.referenced:
          pattern: ^DynamicRemotePluginOptions$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-webpack'
    - or:
      - frontend.referenced:
          pattern: ^PluginModuleFederationSettings$
          location: TYPE_REFERENCE
          from: '@openshift/dynamic-plugin-sdk-webpack'
      - frontend.referenced:
          pattern: ^PluginModuleFederationSettings$
          location: IMPORT
          from: '@openshift/dynamic-plugin-sdk-webpack'
- ruleID: semver-openshift-dynamic-plugin-sdk-utils-constant-removed-llmassisted-combined
  labels:
  - source=semver-analyzer
  - change-type=removed
  - kind=constant
  - has-codemod=true
  - package=@openshift/dynamic-plugin-sdk-utils
  effort: 3
  category: mandatory
  description: 96 constants from @openshift/dynamic-plugin-sdk-utils have breaking changes
  message: |-
    96 constants from `@openshift/dynamic-plugin-sdk-utils` had breaking changes (removed).
    Affected constants include: DetailsItemList, HorizontalNavTabs, OverviewPage, Tab, Td and 91 more.

    Per-constant changes:
      - DetailsItemList: removed (was constant: DetailsItemList: React_3.FC)
      - DetailsItemList: removed (was variable: DetailsItemList)
      - DetailsItemList: removed (was variable: DetailsItemList)
      - HorizontalNavTabs: removed (was variable: HorizontalNavTabs)
      - OverviewPage: removed (was constant: OverviewPage: React_3.FC<OverViewPageProps>)
      - OverviewPage: removed (was variable: OverviewPage)
      - OverviewPage: removed (was variable: OverviewPage)
      - Tab: removed (was variable: Tab)
      - Td: removed (was constant: Td: ForwardRefExoticComponent<Omit<TdProps, "ref"> & RefAttributes<HTMLTableCellElement> & { className?: string | undefined }>)
      - DetailsPageProps: removed (was type alias: DetailsPageProps: HorizontalNavProps & DetailsPageHeaderProps)
      ... and 86 more
  when:
    frontend.referenced:
      pattern: ^(AccessDenied|Action|ActionButtonProp|ActionButtonsProps|ActionCTA|ActionMenuOptions|ActionMenuProps|ActionMenuVariant|BreadcrumbProp|BreadcrumbsProps|DetailsItemList|DetailsItemProps|DetailsPageHeaderProps|DetailsPageProps|FilterItem|GettingStartedCardProps|GroupedActions|HorizontalNavProps|HorizontalNavTabs|HrefForLabels|LabelListProps|ListViewProps|LoadError|OverViewPageProps|OverviewPage|PageHeading|ResourceSummaryProps|RowProps|Tab|TableColumn|TablePagination|Td|VirtualizedTableProps|WithRouterProps|WithScrollContainer|compareData|default|getParentScrollableElement|withRouter)$
      location: IMPORT
      from: '@openshift/dynamic-plugin-sdk-utils'
